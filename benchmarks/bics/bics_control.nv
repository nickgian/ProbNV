(* models bgp, ospf, static routes *)

type prefix = (int, int6) (* IP prefix; tuple of (address, length) *)
type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
    connected:option[int8]; (* Just track administrative distance *)
    static:option[int8]; (* Just track administrative distance *)
    ospf:option[ospfType];
    bgp:option[bgpType];
    selected:option[int2]; (* Which protocol has the best route *) }
type attribute = rib

(* Definitions for the "best" field *)
let p_CONNECTED = 0u2
let p_STATIC = 1u2
let p_OSPF = 2u2
let p_BGP = 3u2

(** Useful helper definitions **)

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

(* Check if the selected protocol is x *)
let isProtocol fib x =
  match fib with
  | None -> false
  | Some y -> x = y

let flipEdge e = 
  match e with  | a~b -> toEdge b a

let getSourceNode e = 
  match e with  | a~b -> a

let min x y = x <u8 y

(* Compute the better of x and y according to f *)
(* Return a boolean (true for x, false for y) for efficiency reasons *)
let pickOption f x y =
  match (x,y) with
  | (None, _) -> false  | (_, None) -> true
  | (Some a, Some b) -> f a b

let pickMinOption = pickOption min

(* OSPF Route ranking: first compare areas, then weights *)
let betterOspf o1 o2 =
  if o1.areaType <u2 o2.areaType then true
  else if o2.areaType <u2 o1.areaType then false
  else if o1.weight <=u16 o2.weight then true else false

let mergeOption f x y =
  match (x,y) with
  | (None, _) -> y  | (_, None) -> x
  | (Some a, Some b) -> Some (f a b)

(* BGP Route ranking: first compare local pref, then path length, then MED. 
       If multipath is disabled then tie-break is arbitrary (normally, the router id should be used) *)
let betterBgp multiPath b1 b2 =
  if b1.lp > b2.lp then b1
  else if b2.lp > b1.lp then b2
  else if b1.aslen < b2.aslen then b1
  else if b2.aslen < b1.aslen then b2
  else if b1.med < b2.med then b1
  else if b1.med > b2.med then b2
  else b1

(* Determine which of the four protocols has the best route by comparing their ADs *)
let best c s o b =
  match (c,s,o,b) with
  (* If no protocol has a route, then we have no route at all *)
  | (None,None,None,None) -> None
  | _ -> 
      (* Otherwise, get administrative distances for osfp and bgp... *)
      let o = match o with | None -> None | Some o -> Some o.ospfAd in
      let b = match b with | None -> None | Some b -> Some b.bgpAd in
      (* ...and figure out which of the protocols has the lowest AD *)
      let (x,p1) = if pickMinOption c s then (c,p_CONNECTED) else (s,p_STATIC) in
      let (y,p2) = if pickMinOption o b then (o,p_OSPF) else (b,p_BGP) in
      Some (if pickMinOption x y then p1 else p2)

(* Compute the best route for each protocol individually, then select the best one *)
let mergeValues bgpMultiPathEnabled x y =
  let c = if (pickMinOption x.connected y.connected) then x.connected else y.connected in
  let s = if (pickMinOption x.static y.static) then x.static else y.static in
  let o = if (pickOption betterOspf x.ospf y.ospf) then x.ospf else y.ospf in
  let b = mergeOption (betterBgp bgpMultiPathEnabled) x.bgp y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues false x y

let init d node =
  match node with
  | 0n -> 
if d = (10.0.0.102/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.33.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 1n -> 
if d = (10.0.0.94/31) || d = (10.0.0.62/31) || d = (10.0.0.110/31) || d = (10.0.0.106/31) || d = (10.0.0.120/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.66.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 2n -> 
if d = (10.0.0.46/31) || d = (10.0.0.44/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.42.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 3n -> 
if d = (10.0.0.126/31) || d = (10.0.0.124/31) || d = (10.0.0.22/31) || d = (10.0.0.96/31) || d = (10.0.0.80/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.31.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 4n -> 
if d = (10.0.0.96/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.52.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 5n -> 
if d = (10.0.0.62/31) || d = (10.0.0.30/31) || d = (10.0.0.56/31) || d = (10.0.0.24/31) || d = (10.0.0.4/31) || d = (10.0.0.64/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.58.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 6n -> 
if d = (10.0.0.118/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.27.0/24) || d = (128.0.7.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 7n -> 
if d = (10.0.0.122/31) || d = (10.0.0.72/31) || d = (10.0.0.112/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.24.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 8n -> 
if d = (10.0.0.76/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.26.0/24) || d = (128.0.5.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 9n -> 
if d = (10.0.0.108/31) || d = (10.0.0.120/31) || d = (10.0.0.104/31) || d = (10.0.0.54/31) || d = (10.0.0.48/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.40.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 10n -> 
if d = (10.0.0.58/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.28.0/24) || d = (128.0.4.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 11n -> 
if d = (10.0.0.14/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.45.0/24) || d = (128.0.10.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 12n -> 
if d = (10.0.0.54/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.62.0/24) || d = (128.0.14.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 13n -> 
if d = (10.0.0.74/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.43.0/24) || d = (128.0.1.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 14n -> 
if d = (10.0.0.94/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.39.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 15n -> 
if d = (10.0.0.56/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.54.0/24) || d = (128.0.9.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 16n -> 
if d = (10.0.0.24/31) || d = (10.0.0.22/31) || d = (10.0.0.20/31) || d = (10.0.0.18/31) || d = (10.0.0.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.22.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 17n -> 
if d = (10.0.0.64/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.55.0/24) || d = (128.0.8.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 18n -> 
if d = (10.0.0.12/31) || d = (10.0.0.90/31) || d = (10.0.0.6/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.36.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 19n -> 
if d = (10.0.0.8/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.47.0/24) || d = (128.0.11.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 20n -> 
if d = (10.0.0.74/31) || d = (10.0.0.100/31) || d = (10.0.0.98/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.35.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 21n -> 
if d = (10.0.0.38/31) || d = (10.0.0.36/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.51.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 22n -> 
if d = (10.0.0.60/31) || d = (10.0.0.66/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.59.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 23n -> 
if d = (10.0.0.52/31) || d = (10.0.0.50/31) || d = (10.0.0.48/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.48.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 24n -> 
if d = (10.0.0.122/31) || d = (10.0.0.10/31) || d = (10.0.0.116/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.67.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 25n -> 
if d = (10.0.0.4/31) || d = (10.0.0.2/31) || d = (10.0.0.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.49.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 26n -> 
if d = (10.0.0.72/31) || d = (10.0.0.70/31) || d = (10.0.0.52/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.44.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 27n -> 
if d = (10.0.0.88/31) || d = (10.0.0.86/31) || d = (10.0.0.38/31) || d = (10.0.0.32/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.64.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 28n -> 
if d = (10.0.0.126/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.32.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 29n -> 
if d = (10.0.0.106/31) || d = (10.0.0.104/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.37.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 30n -> 
if d = (10.0.0.60/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.60.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 31n -> 
if d = (10.0.0.14/31) || d = (10.0.0.12/31) || d = (10.0.0.16/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.61.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 32n -> 
if d = (10.0.0.44/31) || d = (10.0.0.124/31) || d = (10.0.0.68/31) || d = (10.0.0.114/31) || d = (10.0.0.82/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.41.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 33n -> 
if d = (10.0.0.68/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.57.0/24) || d = (128.0.0.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 34n -> 
if d = (10.0.0.30/31) || d = (10.0.0.28/31) || d = (10.0.0.26/31) || d = (10.0.0.32/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.25.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 35n -> 
if d = (10.0.0.92/31) || d = (10.0.0.34/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.65.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 36n -> 
if d = (10.0.0.110/31) || d = (10.0.0.88/31) || d = (10.0.0.102/31) || d = (10.0.0.118/31) || d = (10.0.0.116/31) || d = (10.0.0.84/31) || d = (10.0.0.50/31) || d = (10.0.0.114/31) || d = (10.0.0.112/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.53.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 37n -> 
if d = (10.0.0.34/31) || d = (10.0.0.16/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.29.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 38n -> 
if d = (10.0.0.46/31) || d = (10.0.0.78/31) || d = (10.0.0.76/31) || d = (10.0.0.58/31) || d = (10.0.0.20/31) || d = (10.0.0.36/31) || d = (10.0.0.84/31) || d = (10.0.0.82/31) || d = (10.0.0.80/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.46.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 39n -> 
if d = (10.0.0.78/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.30.0/24) || d = (128.0.6.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 40n -> 
if d = (10.0.0.108/31) || d = (10.0.0.42/31) || d = (10.0.0.40/31) || d = (10.0.0.100/31) || d = (10.0.0.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.50.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 41n -> 
if d = (10.0.0.28/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.21.0/24) || d = (128.0.2.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 42n -> 
if d = (10.0.0.26/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.20.0/24) || d = (128.0.3.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 43n -> 
if d = (10.0.0.92/31) || d = (10.0.0.90/31) || d = (10.0.0.70/31) || d = (10.0.0.66/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.38.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 44n -> 
if d = (10.0.0.18/31) || d = (10.0.0.98/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.56.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 45n -> 
if d = (10.0.0.86/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.63.0/24) || d = (128.0.15.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 46n -> 
if d = (10.0.0.40/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (128.0.12.0/24) || d = (200.1.23.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 47n -> 
if d = (10.0.0.42/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (128.0.13.0/24) || d = (200.1.19.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 48n -> 
if d = (10.0.0.10/31) || d = (10.0.0.8/31) || d = (10.0.0.6/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (200.1.34.0/24) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | _ -> {connected=None; static=None; ospf=None; bgp=None; selected=None;}

 let transferOspf edge o =
   match o with
   | None -> None
   | Some o -> (
     match edge with
     | _ -> None)

 let transferBgp d e x0 =
  let (prefix, prefixLen) = d in
let prot = x0.selected in
 match e with
   | (0~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.33.0) && (prefix < 200.1.34.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (1~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.66.0) && (prefix < 200.1.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (1~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.66.0) && (prefix < 200.1.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (1~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.66.0) && (prefix < 200.1.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.14.0) && (prefix < 128.0.15.0))) then
(Some {b with lp= 101; comms= b.comms[6553600:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (1~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.66.0) && (prefix < 200.1.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (1~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.66.0) && (prefix < 200.1.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (2~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.42.0) && (prefix < 200.1.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (2~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.42.0) && (prefix < 200.1.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (3~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.31.0) && (prefix < 200.1.32.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (3~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.31.0) && (prefix < 200.1.32.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (3~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.31.0) && (prefix < 200.1.32.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (3~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.31.0) && (prefix < 200.1.32.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (3~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.31.0) && (prefix < 200.1.32.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.4.0) && (prefix < 128.0.5.0))) then
(Some {b with lp= 101; comms= b.comms[6553613:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (4~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.52.0) && (prefix < 200.1.53.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (5~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 101; comms= b.comms[6553601:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (5~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 2; comms= b.comms[6553603:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 2; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (5~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 2; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (5~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (5~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (5~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.58.0) && (prefix < 200.1.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (6~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.27.0) && (prefix < 200.1.28.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.7.0) && (prefix < 128.0.8.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553607:= true]; })))
   | (7~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.24.0) && (prefix < 200.1.25.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (7~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.24.0) && (prefix < 200.1.25.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 101; comms= b.comms[6553608:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (7~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.24.0) && (prefix < 200.1.25.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.7.0) && (prefix < 128.0.8.0))) then
(Some {b with lp= 101; comms= b.comms[6553608:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (8~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.26.0) && (prefix < 200.1.27.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.5.0) && (prefix < 128.0.6.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553605:= true]; })))
   | (9~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.40.0) && (prefix < 200.1.41.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.14.0) && (prefix < 128.0.15.0))) then
(Some {b with lp= 101; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (9~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.40.0) && (prefix < 200.1.41.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 2; comms= b.comms[6553613:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.14.0) && (prefix < 128.0.15.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (9~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.40.0) && (prefix < 200.1.41.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (9~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.40.0) && (prefix < 200.1.41.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (9~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.40.0) && (prefix < 200.1.41.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (10~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.28.0) && (prefix < 200.1.29.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.4.0) && (prefix < 128.0.5.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553604:= true]; })))
   | (11~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.45.0) && (prefix < 200.1.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553610:= true]; })))
   | (12~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.62.0) && (prefix < 200.1.63.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.14.0) && (prefix < 128.0.15.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553614:= true]; })))
   | (13~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.43.0) && (prefix < 200.1.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553601:= true]; })))
   | (14~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.39.0) && (prefix < 200.1.40.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (15~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.54.0) && (prefix < 200.1.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553609:= true]; })))
   | (16~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.22.0) && (prefix < 200.1.23.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 101; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (16~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.22.0) && (prefix < 200.1.23.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (16~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.22.0) && (prefix < 200.1.23.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.5.0) && (prefix < 128.0.6.0))) then
(Some {b with lp= 101; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (16~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.22.0) && (prefix < 200.1.23.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553605:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (16~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.22.0) && (prefix < 200.1.23.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (17~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.55.0) && (prefix < 200.1.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553608:= true]; })))
   | (18~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.36.0) && (prefix < 200.1.37.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (18~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.36.0) && (prefix < 200.1.37.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 101; comms= b.comms[6553602:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (18~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.36.0) && (prefix < 200.1.37.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (19~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.47.0) && (prefix < 200.1.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553611:= true]; })))
   | (20~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.35.0) && (prefix < 200.1.36.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 101; comms= b.comms[6553608:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (20~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.35.0) && (prefix < 200.1.36.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (20~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.35.0) && (prefix < 200.1.36.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 2; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (21~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.51.0) && (prefix < 200.1.52.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553602:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 2; comms= b.comms[6553605:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (21~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.51.0) && (prefix < 200.1.52.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (22~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.59.0) && (prefix < 200.1.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (22~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.59.0) && (prefix < 200.1.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (23~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.48.0) && (prefix < 200.1.49.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (23~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.48.0) && (prefix < 200.1.49.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (23~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.48.0) && (prefix < 200.1.49.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (24~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.67.0) && (prefix < 200.1.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (24~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.67.0) && (prefix < 200.1.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (24~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.67.0) && (prefix < 200.1.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (25~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.49.0) && (prefix < 200.1.50.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (25~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.49.0) && (prefix < 200.1.50.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 101; comms= b.comms[6553602:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (25~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.49.0) && (prefix < 200.1.50.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (26~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.44.0) && (prefix < 200.1.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 101; comms= b.comms[6553609:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (26~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.44.0) && (prefix < 200.1.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (26~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.44.0) && (prefix < 200.1.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (27~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.64.0) && (prefix < 200.1.65.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (27~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.64.0) && (prefix < 200.1.65.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (27~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.64.0) && (prefix < 200.1.65.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 2; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (27~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.64.0) && (prefix < 200.1.65.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (28~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.32.0) && (prefix < 200.1.33.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (29~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.37.0) && (prefix < 200.1.38.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (29~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.37.0) && (prefix < 200.1.38.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 101; comms= b.comms[6553609:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (30~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.60.0) && (prefix < 200.1.61.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (31~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.61.0) && (prefix < 200.1.62.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (31~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.61.0) && (prefix < 200.1.62.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (31~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.61.0) && (prefix < 200.1.62.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (32~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.41.0) && (prefix < 200.1.42.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (32~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.41.0) && (prefix < 200.1.42.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.6.0) && (prefix < 128.0.7.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (32~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.41.0) && (prefix < 200.1.42.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.1.0))) then
(Some {b with lp= 100; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (32~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.41.0) && (prefix < 200.1.42.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.1.0))) then
(Some {b with lp= 101; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (32~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.41.0) && (prefix < 200.1.42.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (33~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.57.0) && (prefix < 200.1.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.1.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553600:= true]; })))
   | (34~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.25.0) && (prefix < 200.1.26.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (34~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.25.0) && (prefix < 200.1.26.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (34~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.25.0) && (prefix < 200.1.26.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (34~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.25.0) && (prefix < 200.1.26.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 3; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553604:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (35~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.65.0) && (prefix < 200.1.66.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (35~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.65.0) && (prefix < 200.1.66.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 101; comms= b.comms[6553608:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (36~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.7.0) && (prefix < 128.0.8.0))) then
(Some {b with lp= 101; comms= b.comms[6553601:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.7.0) && (prefix < 128.0.8.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (36~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.53.0) && (prefix < 200.1.54.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.1.0))) then
(Some {b with lp= 100; comms= b.comms[6553601:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (37~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.29.0) && (prefix < 200.1.30.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (37~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.29.0) && (prefix < 200.1.30.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.8.0) && (prefix < 128.0.9.0))) then
(Some {b with lp= 101; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (38~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.6.0) && (prefix < 128.0.7.0))) then
(Some {b with lp= 101; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (38~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 101; comms= b.comms[6553614:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.5.0) && (prefix < 128.0.6.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.4.0) && (prefix < 128.0.5.0))) then
(Some {b with lp= 101; comms= b.comms[6553604:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (38~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.5.0) && (prefix < 128.0.6.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.4.0) && (prefix < 128.0.5.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (38~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.46.0) && (prefix < 200.1.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.6.0) && (prefix < 128.0.7.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (39~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.30.0) && (prefix < 200.1.31.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.6.0) && (prefix < 128.0.7.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553606:= true]; })))
   | (40~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.50.0) && (prefix < 200.1.51.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.9.0) && (prefix < 128.0.10.0))) then
(Some {b with lp= 101; comms= b.comms[6553610:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 101; comms= b.comms[6553606:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (40~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.50.0) && (prefix < 200.1.51.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (40~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.50.0) && (prefix < 200.1.51.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (40~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.50.0) && (prefix < 200.1.51.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 101; comms= b.comms[6553602:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.1.0) && (prefix < 128.0.2.0))) then
(Some {b with lp= 101; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 100; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (40~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.50.0) && (prefix < 200.1.51.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 2; comms= b.comms[6553605:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (41~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.21.0) && (prefix < 200.1.22.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.2.0) && (prefix < 128.0.3.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553602:= true]; })))
   | (42~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.20.0) && (prefix < 200.1.21.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.3.0) && (prefix < 128.0.4.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553603:= true]; })))
   | (43~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.38.0) && (prefix < 200.1.39.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (43~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.38.0) && (prefix < 200.1.39.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (43~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.38.0) && (prefix < 200.1.39.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 101; comms= b.comms[6553615:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (43~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.38.0) && (prefix < 200.1.39.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.10.0) && (prefix < 128.0.11.0))) then
(Some {b with lp= 101; comms= b.comms[6553611:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (44~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.56.0) && (prefix < 200.1.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 101; comms= b.comms[6553607:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (44~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.56.0) && (prefix < 200.1.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (45~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.63.0) && (prefix < 200.1.64.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.15.0) && (prefix < 128.0.16.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553615:= true]; })))
   | (46~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.23.0) && (prefix < 200.1.24.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.12.0) && (prefix < 128.0.13.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553612:= true]; })))
   | (47~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.19.0) && (prefix < 200.1.20.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if ((prefixLen = 24u6) && ((prefix >= 128.0.13.0) && (prefix < 128.0.14.0))) then
(Some {b with lp= 100; aslen= b.aslen + 2 + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           (Some {b with lp= 101; comms= b.comms[6553613:= true]; })))
   | (48~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.34.0) && (prefix < 200.1.35.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 101; comms= b.comms[6553612:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))
   | (48~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.34.0) && (prefix < 200.1.35.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
      Some b))
   | (48~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 200.1.34.0) && (prefix < 200.1.35.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
         (match b with
         | None -> None
         | Some b ->
           if ((prefixLen = 24u6) && ((prefix >= 128.0.11.0) && (prefix < 128.0.12.0))) then
(Some {b with lp= 2; comms= b.comms[6553602:= true]; })
 else
if ((prefixLen = 20u6) && ((prefix >= 128.0.0.0) && (prefix < 128.0.16.0))) then
None
 else
(Some b)))


let trans d edge x = 
  let o = transferOspf edge x.ospf in
  let b = transferBgp d edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let nodes = (49, {
  3n:"london";
  5n:"geneva";
  6n:"peerfrankfurt_7";
  7n:"praha";
  8n:"peerbrussels_5";
  10n:"peerbrussels_4";
  12n:"peermilan_14";
  18n:"bucharest";
  20n:"barcelona";
  23n:"vienna";
  24n:"warsaw";
  25n:"lyon";
  26n:"bratislava";
  28n:"lisbon";
  29n:"roma";
  30n:"ljubjana";
  31n:"istanbul";
  32n:"amsterdam";
  34n:"basel";
  35n:"sofia";
  37n:"athens";
  38n:"brussels";
  39n:"peerbrussels_6";
  43n:"budapest";
  44n:"madrid";
  45n:"peerstrasbourg_15";
  46n:"peermarseille_12";
  47n:"peermarseille_13";
  48n:"kiev";
  0n:"stockholm";
  1n:"zurich";
  2n:"rotterdam";
  4n:"dublin";
  9n:"milan";
  11n:"peeristanbul_10";
  13n:"peerbarcelona_1";
  14n:"vaduz";
  15n:"peergeneva_9";
  16n:"paris";
  17n:"peergeneva_8";
  19n:"peerkiev_11";
  21n:"luxembourg";
  22n:"zagreb";
  27n:"strasbourg";
  33n:"peeramsterdam_0";
  36n:"frankfurt";
  40n:"marseille";
  41n:"peerbasel_2";
  42n:"peerbasel_3";
 })

let edges = {
  19-48; (*peerkiev_11,FastEthernet0/0 --> kiev,FastEthernet0/1*)
  36-23; (*frankfurt,FastEthernet3/0 --> vienna,FastEthernet0/1*)
  30-22; (*ljubjana,FastEthernet0/0 --> zagreb,FastEthernet0/1*)
  38-21; (*brussels,FastEthernet1/1 --> luxembourg,FastEthernet0/0*)
  38-39; (*brussels,FastEthernet3/1 --> peerbrussels_6,FastEthernet0/0*)
  37-35; (*athens,FastEthernet0/1 --> sofia,FastEthernet0/0*)
  22-30; (*zagreb,FastEthernet0/1 --> ljubjana,FastEthernet0/0*)
  40-25; (*marseille,FastEthernet0/1 --> lyon,FastEthernet0/1*)
  10-38; (*peerbrussels_4,FastEthernet0/0 --> brussels,FastEthernet2/1*)
  24-48; (*warsaw,FastEthernet0/1 --> kiev,FastEthernet1/0*)
  45-27; (*peerstrasbourg_15,FastEthernet0/0 --> strasbourg,FastEthernet1/1*)
  34-5; (*basel,FastEthernet0/0 --> geneva,FastEthernet0/0*)
  25-16; (*lyon,FastEthernet1/0 --> paris,FastEthernet1/1*)
  31-11; (*istanbul,FastEthernet1/0 --> peeristanbul_10,FastEthernet0/0*)
  1-9; (*zurich,FastEthernet1/0 --> milan,FastEthernet2/0*)
  18-43; (*bucharest,FastEthernet0/0 --> budapest,FastEthernet0/1*)
  38-36; (*brussels,FastEthernet0/1 --> frankfurt,FastEthernet0/1*)
  32-38; (*amsterdam,FastEthernet0/0 --> brussels,FastEthernet0/0*)
  20-44; (*barcelona,FastEthernet0/0 --> madrid,FastEthernet0/0*)
  43-22; (*budapest,FastEthernet1/1 --> zagreb,FastEthernet0/0*)
  1-5; (*zurich,FastEthernet0/1 --> geneva,FastEthernet2/1*)
  9-29; (*milan,FastEthernet1/0 --> roma,FastEthernet0/0*)
  3-16; (*london,FastEthernet2/0 --> paris,FastEthernet1/0*)
  23-26; (*vienna,FastEthernet0/0 --> bratislava,FastEthernet1/0*)
  41-34; (*peerbasel_2,FastEthernet0/0 --> basel,FastEthernet0/1*)
  5-25; (*geneva,FastEthernet0/1 --> lyon,FastEthernet0/0*)
  48-18; (*kiev,FastEthernet0/0 --> bucharest,FastEthernet1/0*)
  16-44; (*paris,FastEthernet2/0 --> madrid,FastEthernet0/1*)
  1-29; (*zurich,FastEthernet1/1 --> roma,FastEthernet0/1*)
  20-40; (*barcelona,FastEthernet0/1 --> marseille,FastEthernet0/0*)
  34-42; (*basel,FastEthernet1/0 --> peerbasel_3,FastEthernet0/0*)
  36-6; (*frankfurt,FastEthernet1/0 --> peerfrankfurt_7,FastEthernet0/0*)
  18-31; (*bucharest,FastEthernet0/1 --> istanbul,FastEthernet0/1*)
  38-10; (*brussels,FastEthernet2/1 --> peerbrussels_4,FastEthernet0/0*)
  5-15; (*geneva,FastEthernet2/0 --> peergeneva_9,FastEthernet0/0*)
  40-47; (*marseille,FastEthernet2/0 --> peermarseille_13,FastEthernet0/0*)
  31-37; (*istanbul,FastEthernet0/0 --> athens,FastEthernet0/0*)
  38-3; (*brussels,FastEthernet1/0 --> london,FastEthernet0/1*)
  36-24; (*frankfurt,FastEthernet3/1 --> warsaw,FastEthernet0/0*)
  48-24; (*kiev,FastEthernet1/0 --> warsaw,FastEthernet0/1*)
  35-43; (*sofia,FastEthernet0/1 --> budapest,FastEthernet1/0*)
  0-36; (*stockholm,FastEthernet0/0 --> frankfurt,FastEthernet2/0*)
  32-33; (*amsterdam,FastEthernet1/1 --> peeramsterdam_0,FastEthernet0/0*)
  14-1; (*vaduz,FastEthernet0/0 --> zurich,FastEthernet2/0*)
  7-36; (*praha,FastEthernet0/1 --> frankfurt,FastEthernet1/1*)
  38-8; (*brussels,FastEthernet3/0 --> peerbrussels_5,FastEthernet0/0*)
  43-26; (*budapest,FastEthernet0/0 --> bratislava,FastEthernet0/0*)
  16-5; (*paris,FastEthernet0/1 --> geneva,FastEthernet1/0*)
  16-3; (*paris,FastEthernet1/0 --> london,FastEthernet2/0*)
  36-32; (*frankfurt,FastEthernet0/0 --> amsterdam,FastEthernet0/1*)
  7-24; (*praha,FastEthernet1/0 --> warsaw,FastEthernet1/0*)
  40-20; (*marseille,FastEthernet0/0 --> barcelona,FastEthernet0/1*)
  20-13; (*barcelona,FastEthernet1/0 --> peerbarcelona_1,FastEthernet0/0*)
  38-2; (*brussels,FastEthernet4/0 --> rotterdam,FastEthernet0/1*)
  22-43; (*zagreb,FastEthernet0/0 --> budapest,FastEthernet1/1*)
  29-1; (*roma,FastEthernet0/1 --> zurich,FastEthernet1/1*)
  5-1; (*geneva,FastEthernet2/1 --> zurich,FastEthernet0/1*)
  15-5; (*peergeneva_9,FastEthernet0/0 --> geneva,FastEthernet2/0*)
  6-36; (*peerfrankfurt_7,FastEthernet0/0 --> frankfurt,FastEthernet1/0*)
  28-3; (*lisbon,FastEthernet0/0 --> london,FastEthernet1/1*)
  48-19; (*kiev,FastEthernet0/1 --> peerkiev_11,FastEthernet0/0*)
  13-20; (*peerbarcelona_1,FastEthernet0/0 --> barcelona,FastEthernet1/0*)
  40-46; (*marseille,FastEthernet1/1 --> peermarseille_12,FastEthernet0/0*)
  46-40; (*peermarseille_12,FastEthernet0/0 --> marseille,FastEthernet1/1*)
  12-9; (*peermilan_14,FastEthernet0/0 --> milan,FastEthernet0/1*)
  5-34; (*geneva,FastEthernet0/0 --> basel,FastEthernet0/0*)
  34-41; (*basel,FastEthernet0/1 --> peerbasel_2,FastEthernet0/0*)
  38-16; (*brussels,FastEthernet2/0 --> paris,FastEthernet0/0*)
  2-32; (*rotterdam,FastEthernet0/0 --> amsterdam,FastEthernet2/0*)
  33-32; (*peeramsterdam_0,FastEthernet0/0 --> amsterdam,FastEthernet1/1*)
  36-27; (*frankfurt,FastEthernet2/1 --> strasbourg,FastEthernet0/1*)
  16-38; (*paris,FastEthernet0/0 --> brussels,FastEthernet2/0*)
  37-31; (*athens,FastEthernet0/0 --> istanbul,FastEthernet0/0*)
  27-21; (*strasbourg,FastEthernet1/0 --> luxembourg,FastEthernet0/1*)
  3-38; (*london,FastEthernet0/1 --> brussels,FastEthernet1/0*)
  17-5; (*peergeneva_8,FastEthernet0/0 --> geneva,FastEthernet1/1*)
  25-5; (*lyon,FastEthernet0/0 --> geneva,FastEthernet0/1*)
  9-40; (*milan,FastEthernet0/0 --> marseille,FastEthernet1/0*)
  32-3; (*amsterdam,FastEthernet1/0 --> london,FastEthernet0/0*)
  32-2; (*amsterdam,FastEthernet2/0 --> rotterdam,FastEthernet0/0*)
  26-7; (*bratislava,FastEthernet0/1 --> praha,FastEthernet0/0*)
  44-20; (*madrid,FastEthernet0/0 --> barcelona,FastEthernet0/0*)
  11-31; (*peeristanbul_10,FastEthernet0/0 --> istanbul,FastEthernet1/0*)
  5-17; (*geneva,FastEthernet1/1 --> peergeneva_8,FastEthernet0/0*)
  38-32; (*brussels,FastEthernet0/0 --> amsterdam,FastEthernet0/0*)
  3-28; (*london,FastEthernet1/1 --> lisbon,FastEthernet0/0*)
  5-16; (*geneva,FastEthernet1/0 --> paris,FastEthernet0/1*)
  39-38; (*peerbrussels_6,FastEthernet0/0 --> brussels,FastEthernet3/1*)
  23-36; (*vienna,FastEthernet0/1 --> frankfurt,FastEthernet3/0*)
  2-38; (*rotterdam,FastEthernet0/1 --> brussels,FastEthernet4/0*)
  24-7; (*warsaw,FastEthernet1/0 --> praha,FastEthernet1/0*)
  36-7; (*frankfurt,FastEthernet1/1 --> praha,FastEthernet0/1*)
  42-34; (*peerbasel_3,FastEthernet0/0 --> basel,FastEthernet1/0*)
  4-3; (*dublin,FastEthernet0/0 --> london,FastEthernet1/0*)
  24-36; (*warsaw,FastEthernet0/0 --> frankfurt,FastEthernet3/1*)
  34-27; (*basel,FastEthernet1/1 --> strasbourg,FastEthernet0/0*)
  7-26; (*praha,FastEthernet0/0 --> bratislava,FastEthernet0/1*)
  16-25; (*paris,FastEthernet1/1 --> lyon,FastEthernet1/0*)
  36-38; (*frankfurt,FastEthernet0/1 --> brussels,FastEthernet0/1*)
  26-23; (*bratislava,FastEthernet1/0 --> vienna,FastEthernet0/0*)
  43-35; (*budapest,FastEthernet1/0 --> sofia,FastEthernet0/1*)
  21-27; (*luxembourg,FastEthernet0/1 --> strasbourg,FastEthernet1/0*)
  26-43; (*bratislava,FastEthernet0/0 --> budapest,FastEthernet0/0*)
  29-9; (*roma,FastEthernet0/0 --> milan,FastEthernet1/0*)
  3-4; (*london,FastEthernet1/0 --> dublin,FastEthernet0/0*)
  27-36; (*strasbourg,FastEthernet0/1 --> frankfurt,FastEthernet2/1*)
  43-18; (*budapest,FastEthernet0/1 --> bucharest,FastEthernet0/0*)
  25-40; (*lyon,FastEthernet0/1 --> marseille,FastEthernet0/1*)
  8-38; (*peerbrussels_5,FastEthernet0/0 --> brussels,FastEthernet3/0*)
  44-16; (*madrid,FastEthernet0/1 --> paris,FastEthernet2/0*)
  31-18; (*istanbul,FastEthernet0/1 --> bucharest,FastEthernet0/1*)
  27-45; (*strasbourg,FastEthernet1/1 --> peerstrasbourg_15,FastEthernet0/0*)
  40-9; (*marseille,FastEthernet1/0 --> milan,FastEthernet0/0*)
  18-48; (*bucharest,FastEthernet1/0 --> kiev,FastEthernet0/0*)
  36-0; (*frankfurt,FastEthernet2/0 --> stockholm,FastEthernet0/0*)
  1-14; (*zurich,FastEthernet2/0 --> vaduz,FastEthernet0/0*)
  9-23; (*milan,FastEthernet1/1 --> vienna,FastEthernet1/0*)
  32-36; (*amsterdam,FastEthernet0/1 --> frankfurt,FastEthernet0/0*)
  27-34; (*strasbourg,FastEthernet0/0 --> basel,FastEthernet1/1*)
  9-12; (*milan,FastEthernet0/1 --> peermilan_14,FastEthernet0/0*)
  1-36; (*zurich,FastEthernet0/0 --> frankfurt,FastEthernet4/0*)
  9-1; (*milan,FastEthernet2/0 --> zurich,FastEthernet1/0*)
  35-37; (*sofia,FastEthernet0/0 --> athens,FastEthernet0/1*)
  3-32; (*london,FastEthernet0/0 --> amsterdam,FastEthernet1/0*)
  21-38; (*luxembourg,FastEthernet0/0 --> brussels,FastEthernet1/1*)
  23-9; (*vienna,FastEthernet1/0 --> milan,FastEthernet1/1*)
  36-1; (*frankfurt,FastEthernet4/0 --> zurich,FastEthernet0/0*)
  47-40; (*peermarseille_13,FastEthernet0/0 --> marseille,FastEthernet2/0*)
}

(*
{london=3, geneva=5, peerfrankfurt_7=6, praha=7, peerbrussels_5=8, peerbrussels_4=10, peermilan_14=12, bucharest=18, barcelona=20, vienna=23, warsaw=24, lyon=25, bratislava=26, lisbon=28, roma=29, ljubjana=30, istanbul=31, amsterdam=32, basel=34, sofia=35, athens=37, brussels=38, peerbrussels_6=39, budapest=43, madrid=44, peerstrasbourg_15=45, peermarseille_12=46, peermarseille_13=47, kiev=48, stockholm=0, zurich=1, rotterdam=2, dublin=4, milan=9, peeristanbul_10=11, peerbarcelona_1=13, vaduz=14, peergeneva_9=15, paris=16, peergeneva_8=17, peerkiev_11=19, luxembourg=21, zagreb=22, strasbourg=27, peeramsterdam_0=33, frankfurt=36, marseille=40, peerbasel_2=41, peerbasel_3=42}*)