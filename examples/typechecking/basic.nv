symbolic x:int

let nodes = 2
let edges = {0=1;}

let init u =
  if u = 1n then 15 else 0

let trans e (v : [M]int) = if 0 < x then v + 1 else 1000

let merge u v1 v2 = if v1 < v2 then v1 else v2

let s = solution(init, trans, merge)

assert ((s 1n) < 16)