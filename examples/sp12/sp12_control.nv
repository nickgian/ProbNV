(* models bgp, ospf, static routes *)

type prefix = (int, int6) (* IP prefix; tuple of (address, length) *)
type ospfType = {ospfAd: int8; weight: int16; areaType:int2; areaId: int;}
type bgpType = {bgpAd: int8; lp: int; aslen: int; med:int; comms:set[int];}
type rib = {
    connected:option[int8]; (* Just track administrative distance *)
    static:option[int8]; (* Just track administrative distance *)
    ospf:option[ospfType];
    bgp:option[bgpType];
    selected:option[int2]; (* Which protocol has the best route *) }
type attribute = rib

(* Definitions for the "best" field *)
let p_CONNECTED = 0u2
let p_STATIC = 1u2
let p_OSPF = 2u2
let p_BGP = 3u2

(** Useful helper definitions **)

let ospfIntraArea = 0u2
let ospfInterArea = 1u2
let ospfE1 = 2u2
let ospfE2 = 3u2

(* Check if the selected protocol is x *)
let isProtocol fib x =
  match fib with
  | None -> false
  | Some y -> x = y

let getSourceNode e = 
  match e with  | a~b -> a

let min x y = x <u8 y

(* Compute the better of x and y according to f *)
(* Return a boolean (true for x, false for y) for efficiency reasons *)
let pickOption f x y =
  match (x,y) with
  | (None, _) -> false  | (_, None) -> true
  | (Some a, Some b) -> f a b

let pickMinOption = pickOption min

(* OSPF Route ranking: first compare areas, then weights *)
let betterOspf o1 o2 =
  if o1.areaType <u2 o2.areaType then true
  else if o2.areaType <u2 o1.areaType then false
  else if o1.weight <=u16 o2.weight then true else false

(* BGP Route ranking: first compare local pref, then path length, then MED *)
let betterBgp b1 b2 =
  if b1.lp > b2.lp then true
  else if b2.lp > b1.lp then false
  else if b1.aslen < b2.aslen then true
  else if b2.aslen < b1.aslen then false
  else if b1.med <= b2.med then true else false

(* Determine which of the four protocols has the best route by comparing their ADs *)
let best c s o b =
  match (c,s,o,b) with
  (* If no protocol has a route, then we have no route at all *)
  | (None,None,None,None) -> None
  | _ -> 
      (* Otherwise, get administrative distances for osfp and bgp... *)
      let o = match o with | None -> None | Some o -> Some o.ospfAd in
      let b = match b with | None -> None | Some b -> Some b.bgpAd in
      (* ...and figure out which of the protocols has the lowest AD *)
      let (x,p1) = if pickMinOption c s then (c,p_CONNECTED) else (s,p_STATIC) in
      let (y,p2) = if pickMinOption o b then (o,p_OSPF) else (b,p_BGP) in
      Some (if pickMinOption x y then p1 else p2)

(* Compute the best route for each protocol individually, then select the best one *)
let mergeValues x y =
  let c = if (pickMinOption x.connected y.connected) then x.connected else y.connected in
  let s = if (pickMinOption x.static y.static) then x.static else y.static in
  let o = if (pickOption betterOspf x.ospf y.ospf) then x.ospf else y.ospf in
  let b = if (pickOption betterBgp x.bgp y.bgp) then x.bgp else y.bgp in
  { connected = c;
    static = s;
    ospf = o;
    bgp = b;
    selected = best c s o b}

let merge node x y = mergeValues x y

let init d node =
  match node with
  | 0n -> 
if d = (70.0.137.0/32) || d = (10.3.92.0/31) || d = (10.1.66.0/31) || d = (10.1.65.0/31) || d = (10.1.64.0/31) || d = (10.1.63.0/31) || d = (10.1.62.0/31) || d = (10.3.80.0/31) || d = (10.1.67.0/31) || d = (10.3.68.0/31) || d = (10.3.44.0/31) || d = (10.3.56.0/31) || d = (10.3.32.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 1n -> 
if d = (10.3.20.0/31) || d = (10.2.252.0/31) || d = (10.3.8.0/31) || d = (10.2.240.0/31) || d = (10.2.216.0/31) || d = (10.2.228.0/31) || d = (70.0.136.0/32) || d = (10.1.61.0/31) || d = (10.1.60.0/31) || d = (10.1.59.0/31) || d = (10.1.58.0/31) || d = (10.1.57.0/31) || d = (10.1.56.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 2n -> 
if d = (10.2.204.0/31) || d = (10.2.192.0/31) || d = (10.2.168.0/31) || d = (10.2.180.0/31) || d = (10.2.156.0/31) || d = (70.0.135.0/32) || d = (10.2.144.0/31) || d = (10.1.55.0/31) || d = (10.1.54.0/31) || d = (10.1.53.0/31) || d = (10.1.52.0/31) || d = (10.1.51.0/31) || d = (10.1.50.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 3n -> 
if d = (10.0.255.0/31) || d = (70.0.129.0/32) || d = (10.1.5.0/31) || d = (70.0.129.0/31) || d = (70.0.129.2/31) || d = (10.1.29.0/31) || d = (10.1.11.0/31) || d = (10.1.23.0/31) || d = (10.1.17.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.129.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 4n -> 
if d = (10.0.252.0/31) || d = (70.0.126.0/32) || d = (10.1.8.0/31) || d = (10.1.2.0/31) || d = (70.0.126.0/31) || d = (70.0.126.2/31) || d = (10.1.26.0/31) || d = (10.1.14.0/31) || d = (10.1.20.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.126.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 5n -> 
if d = (10.0.254.0/31) || d = (70.0.128.0/32) || d = (10.1.4.0/31) || d = (70.0.128.0/31) || d = (70.0.128.2/31) || d = (10.1.28.0/31) || d = (10.1.16.0/31) || d = (10.1.10.0/31) || d = (10.1.22.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.128.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 6n -> 
if d = (10.0.253.0/31) || d = (70.0.127.0/32) || d = (10.1.3.0/31) || d = (70.0.127.0/31) || d = (70.0.127.2/31) || d = (10.1.27.0/31) || d = (10.1.15.0/31) || d = (10.1.9.0/31) || d = (10.1.21.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.127.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 7n -> 
if d = (70.0.134.0/32) || d = (10.2.120.0/31) || d = (10.2.132.0/31) || d = (10.2.108.0/31) || d = (10.2.84.0/31) || d = (10.2.96.0/31) || d = (10.2.72.0/31) || d = (10.1.49.0/31) || d = (10.1.48.0/31) || d = (10.1.47.0/31) || d = (10.1.46.0/31) || d = (10.1.45.0/31) || d = (10.1.44.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 8n -> 
if d = (10.2.0.0/31) || d = (10.2.12.0/31) || d = (70.0.133.0/32) || d = (10.1.42.0/31) || d = (10.1.43.0/31) || d = (10.2.60.0/31) || d = (10.2.36.0/31) || d = (10.1.41.0/31) || d = (10.1.40.0/31) || d = (10.1.39.0/31) || d = (10.1.38.0/31) || d = (10.2.48.0/31) || d = (10.2.24.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 9n -> 
if d = (10.1.244.0/31) || d = (10.1.232.0/31) || d = (10.1.208.0/31) || d = (10.1.220.0/31) || d = (10.1.196.0/31) || d = (10.1.184.0/31) || d = (70.0.132.0/32) || d = (10.1.33.0/31) || d = (10.1.32.0/31) || d = (10.1.35.0/31) || d = (10.1.34.0/31) || d = (10.1.37.0/31) || d = (10.1.36.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 10n -> 
if d = (10.2.7.0/31) || d = (10.1.251.0/31) || d = (10.0.117.0/31) || d = (10.0.116.0/31) || d = (10.0.119.0/31) || d = (10.0.118.0/31) || d = (10.0.115.0/31) || d = (10.0.114.0/31) || d = (70.0.73.0/32) || d = (10.2.55.0/31) || d = (10.2.43.0/31) || d = (10.2.19.0/31) || d = (10.2.31.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 11n -> 
if d = (70.0.131.0/32) || d = (10.1.7.0/31) || d = (10.1.1.0/31) || d = (70.0.131.0/31) || d = (70.0.131.2/31) || d = (10.1.31.0/31) || d = (10.1.25.0/31) || d = (10.1.13.0/31) || d = (10.1.19.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.131.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 12n -> 
if d = (10.1.227.0/31) || d = (10.1.239.0/31) || d = (10.1.215.0/31) || d = (10.1.191.0/31) || d = (10.1.203.0/31) || d = (10.1.179.0/31) || d = (10.0.111.0/31) || d = (10.0.110.0/31) || d = (10.0.109.0/31) || d = (10.0.108.0/31) || d = (10.0.113.0/31) || d = (10.0.112.0/31) || d = (70.0.72.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 13
0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 60n -> 
if d = (10.1.240.0/31) || d = (10.1.228.0/31) || d = (10.1.216.0/31) || d = (10.1.192.0/31) || d = (10.1.204.0/31) || d = (10.1.180.0/31) || d = (10.0.149.0/31) || d = (10.0.147.0/31) || d = (10.0.146.0/31) || d = (10.0.145.0/31) || d = (10.0.144.0/31) || d = (10.0.148.0/31) || d = (70.0.84.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 61n -> 
if d = (10.1.66.0/31) || d = (10.1.60.0/31) || d = (10.1.48.0/31) || d = (10.1.42.0/31) || d = (10.1.54.0/31) || d = (10.1.36.0/31) || d = (70.0.142.2/31) || d = (70.0.142.0/32) || d = (70.0.142.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.142.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 62n -> 
if d = (10.1.65.0/31) || d = (10.1.59.0/31) || d = (10.1.47.0/31) || d = (10.1.53.0/31) || d = (10.1.41.0/31) || d = (10.1.35.0/31) || d = (70.0.141.2/31) || d = (70.0.141.0/32) || d = (70.0.141.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.141.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 63n -> 
if d = (70.0.90.0/31) || d = (70.0.90.0/32) || d = (70.0.90.2/31) || d = (10.0.168.0/31) || d = (10.0.174.0/31) || d = (10.0.156.0/31) || d = (10.0.150.0/31) || d = (10.0.162.0/31) || d = (10.0.144.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.90.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 64n -> 
if d = (70.0.95.0/32) || d = (70.0.95.0/31) || d = (70.0.95.2/31) || d = (10.0.173.0/31) || d = (10.0.167.0/31) || d = (10.0.179.0/31) || d = (10.0.155.0/31) || d = (10.0.149.0/31) || d = (10.0.161.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.95.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 65n -> 
if d = (10.0.177.0/31) || d = (10.0.176.0/31) || d = (10.0.175.0/31) || d = (10.0.174.0/31) || d = (10.0.179.0/31) || d = (10.0.178.0/31) || d = (70.0.89.0/32) || d = (10.3.76.0/31) || d = (10.3.88.0/31) || d = (10.3.64.0/31) || d = (10.3.52.0/31) || d = (10.3.28.0/31) || d = (10.3.40.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 66n -> 
if d = (10.0.156.0/31) || d = (10.0.159.0/31) || d = (10.0.158.0/31) || d = (10.0.157.0/31) || d = (10.0.161.0/31) || d = (10.0.160.0/31) || d = (10.2.116.0/31) || d = (10.2.128.0/31) || d = (10.2.104.0/31) || d = (70.0.86.0/32) || d = (10.2.92.0/31) || d = (10.2.68.0/31) || d = (10.2.80.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 67n -> 
if d = (70.0.91.0/32) || d = (70.0.91.0/31) || d = (70.0.91.2/31) || d = (10.0.169.0/31) || d = (10.0.175.0/31) || d = (10.0.151.0/31) || d = (10.0.163.0/31) || d = (10.0.157.0/31) || d = (10.0.145.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.91.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 68n -> 
if d = (10.2.8.0/31) || d = (10.1.252.0/31) || d = (10.0.153.0/31) || d = (10.0.152.0/31) || d = (10.0.151.0/31) || d = (10.0.150.0/31) || d = (10.0.155.0/31) || d = (10.0.154.0/31) || d = (70.0.85.0/32) || d = (10.2.56.0/31) || d = (10.2.44.0/31) || d = (10.2.20.0/31) || d = (10.2.32.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 69n -> 
if d = (70.0.92.0/32) || d = (70.0.92.0/31) || d = (70.0.92.2/31) || d = (10.0.170.0/31) || d = (10.0.176.0/31) || d = (10.0.152.0/31) || d = (10.0.164.0/31) || d = (10.0.158.0/31) || d = (10.0.146.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.92.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  |
0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 98n -> 
if d = (10.3.10.0/31) || d = (10.3.22.0/31) || d = (10.2.254.0/31) || d = (10.2.242.0/31) || d = (10.2.218.0/31) || d = (10.2.230.0/31) || d = (70.0.160.0/32) || d = (10.1.132.0/31) || d = (10.1.131.0/31) || d = (10.1.130.0/31) || d = (10.1.129.0/31) || d = (10.1.128.0/31) || d = (10.1.133.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 99n -> 
if d = (10.3.44.0/31) || d = (10.3.42.0/31) || d = (10.3.47.0/31) || d = (10.3.46.0/31) || d = (10.3.45.0/31) || d = (10.3.43.0/31) || d = (70.0.31.0/32) || d = (10.3.40.0/31) || d = (10.3.37.0/31) || d = (10.3.39.0/31) || d = (10.3.36.0/31) || d = (10.3.41.0/31) || d = (10.3.38.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 100n -> 
if d = (10.3.24.0/31) || d = (70.0.30.0/32) || d = (10.3.28.0/31) || d = (10.3.32.0/31) || d = (10.3.25.0/31) || d = (10.3.30.0/31) || d = (10.3.27.0/31) || d = (10.3.33.0/31) || d = (10.3.31.0/31) || d = (10.3.29.0/31) || d = (10.3.26.0/31) || d = (10.3.35.0/31) || d = (10.3.34.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 101n -> 
if d = (10.1.97.0/31) || d = (10.1.91.0/31) || d = (10.1.103.0/31) || d = (10.1.79.0/31) || d = (10.1.85.0/31) || d = (10.1.73.0/31) || d = (70.0.155.0/31) || d = (70.0.155.0/32) || d = (70.0.155.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.155.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 102n -> 
if d = (10.1.96.0/31) || d = (10.1.102.0/31) || d = (10.1.78.0/31) || d = (10.1.90.0/31) || d = (10.1.84.0/31) || d = (10.1.72.0/31) || d = (70.0.154.0/31) || d = (70.0.154.0/32) || d = (70.0.154.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.154.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 103n -> 
if d = (10.1.128.0/31) || d = (10.1.134.0/31) || d = (10.1.110.0/31) || d = (10.1.122.0/31) || d = (10.1.116.0/31) || d = (10.1.104.0/31) || d = (70.0.162.0/32) || d = (70.0.162.0/31) || d = (70.0.162.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.162.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 104n -> 
if d = (10.1.124.0/31) || d = (10.1.130.0/31) || d = (10.1.136.0/31) || d = (10.1.112.0/31) || d = (10.1.118.0/31) || d = (10.1.106.0/31) || d = (70.0.164.0/32) || d = (70.0.164.0/31) || d = (70.0.164.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.164.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 105n -> 
if d = (10.1.129.0/31) || d = (10.1.135.0/31) || d = (10.1.111.0/31) || d = (10.1.123.0/31) || d = (10.1.117.0/31) || d = (10.1.105.0/31) || d = (70.0.163.0/32) || d = (70.0.163.0/31) || d = (70.0.163.2/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.163.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 106n -> 
if d = (70.0.170.0/32) || d = (10.1.157.0/31) || d = (10.1.156.0/31) || d = (10.1.155.0/31) || d = (10.1.154.0/31) || d = (10.1.153.0/31) || d = (10.1.152.0/31) || d = (10.2.135.0/31) || d = (10.2.123.0/31) || d = (10.2.111.0/31) || d = (10.2.87.0/31) || d = (10.2.99.0/31) || d = (10.2.75.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 107n -> 
if d = (10.3.92.0/31) || d = (10.3.95.0/31) || d = (10.3.94.0/31) || d = (10.3.93.0/31) || d = (10.3.91.0/31) || d = (10.3.88.0/31) || d = (10.3.85.0/31) || d = (10.3.90.0/31) || d = (10.3.87.0/31) || d = (10.3.84.0/31) || d = (10.3.89.0/31) || d = (10.3.86.0/31) || d = (70.0.35.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  |
0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 172n -> 
if d = (10.2.134.0/31) || d = (10.2.133.0/31) || d = (10.2.135.0/31) || d = (10.2.131.0/31) || d = (10.2.128.0/31) || d = (10.2.125.0/31) || d = (10.2.130.0/31) || d = (10.2.127.0/31) || d = (10.2.124.0/31) || d = (10.2.126.0/31) || d = (10.2.132.0/31) || d = (10.2.129.0/31) || d = (70.0.17.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 173n -> 
if d = (10.2.119.0/31) || d = (10.2.116.0/31) || d = (10.2.122.0/31) || d = (10.2.118.0/31) || d = (10.2.121.0/31) || d = (10.2.123.0/31) || d = (10.2.120.0/31) || d = (10.2.117.0/31) || d = (10.2.113.0/31) || d = (10.2.115.0/31) || d = (10.2.112.0/31) || d = (10.2.114.0/31) || d = (70.0.16.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 174n -> 
if d = (10.2.107.0/31) || d = (10.2.104.0/31) || d = (10.2.101.0/31) || d = (10.2.106.0/31) || d = (10.2.103.0/31) || d = (10.2.100.0/31) || d = (10.2.102.0/31) || d = (10.2.105.0/31) || d = (10.2.110.0/31) || d = (10.2.109.0/31) || d = (10.2.111.0/31) || d = (10.2.108.0/31) || d = (70.0.15.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 175n -> 
if d = (10.2.89.0/31) || d = (10.2.88.0/31) || d = (10.2.90.0/31) || d = (10.2.95.0/31) || d = (10.2.92.0/31) || d = (10.2.98.0/31) || d = (10.2.94.0/31) || d = (10.2.97.0/31) || d = (10.2.91.0/31) || d = (10.2.99.0/31) || d = (10.2.96.0/31) || d = (10.2.93.0/31) || d = (70.0.14.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 176n -> 
if d = (10.2.83.0/31) || d = (10.2.86.0/31) || d = (10.2.85.0/31) || d = (10.2.87.0/31) || d = (10.2.84.0/31) || d = (10.2.80.0/31) || d = (10.2.77.0/31) || d = (10.2.82.0/31) || d = (10.2.79.0/31) || d = (10.2.76.0/31) || d = (10.2.78.0/31) || d = (10.2.81.0/31) || d = (70.0.13.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 177n -> 
if d = (10.1.225.0/31) || d = (10.1.237.0/31) || d = (10.1.213.0/31) || d = (10.1.201.0/31) || d = (10.1.177.0/31) || d = (10.1.189.0/31) || d = (10.0.39.0/31) || d = (10.0.38.0/31) || d = (10.0.37.0/31) || d = (10.0.36.0/31) || d = (10.0.41.0/31) || d = (10.0.40.0/31) || d = (70.0.48.0/32) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 178n -> 
if d = (10.0.54.0/31) || d = (70.0.54.0/31) || d = (10.0.66.0/31) || d = (10.0.60.0/31) || d = (70.0.54.0/32) || d = (10.0.36.0/31) || d = (70.0.54.2/31) || d = (10.0.48.0/31) || d = (10.0.42.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else if d = (70.0.54.0/24) then
    let c = None in
    let s = Some 1u8 in
    let o = None in
    let b = Some {bgpAd=20u8; lp=100; aslen=0; med=80; comms={}} in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | 179n -> 
if d = (10.3.24.0/31) || d = (10.3.84.0/31) || d = (10.0.35.0/31) || d = (10.0.34.0/31) || d = (10.3.60.0/31) || d = (10.3.72.0/31) || d = (70.0.41.0/32) || d = (10.3.48.0/31) || d = (10.0.33.0/31) || d = (10.0.32.0/31) || d = (10.0.31.0/31) || d = (10.0.30.0/31) || d = (10.3.36.0/31) then
    let c = Some 0u8 in
    let s = None in
    let o = None in
    let b = None in
    let fib = best c s o b in
      {connected=c; static=s; ospf=o; bgp=b; selected=fib;}
     else {connected=None; static=None; ospf=None; bgp=None; selected=None;}
  | _ -> {connected=None; static=None; ospf=None; bgp=None; selected=None;}

 let transferOspf edge o =
   match o with
   | None -> None
   | Some o -> (
     match edge with
     | _ -> None)

 let transferBgp d e x0 =
  let (prefix, prefixLen) = d in
let prot = x0.selected in
 match e with
   | (0~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (0~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (1~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (2~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (3~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.129.0) && (prefix < 70.0.130.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (4~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.126.0) && (prefix < 70.0.127.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (5~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.128.0) && (prefix < 70.0.129.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (6~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.127.0) && (prefix < 70.0.128.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (7~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (8~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~56) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~62) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~55) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~39) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~59) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~61) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (9~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (10~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (11~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.131.0) && (prefix < 70.0.132.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (12~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (13~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.130.0) && (prefix < 70.0.131.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (14~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (15~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (16~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~50) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~93) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~48) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~52) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~46) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (17~92) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (18~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (19~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (20~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.119.0) && (prefix < 70.0.120.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (21~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.118.0) && (prefix < 70.0.119.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (22~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.115.0) && (prefix < 70.0.116.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (23~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.114.0) && (prefix < 70.0.115.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (24~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.117.0) && (prefix < 70.0.118.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (25~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.116.0) && (prefix < 70.0.117.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (26~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (27~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (28~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~13) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~4) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~6) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~11) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~3) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~5) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (29~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (30~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (31~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (32~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (33~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (34~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~75) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~108) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~78) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~77) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~109) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (35~79) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (36~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (37~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (38~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (39~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.143.0) && (prefix < 70.0.144.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (40~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (41~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.151.0) && (prefix < 70.0.152.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (42~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.150.0) && (prefix < 70.0.151.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (43~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.153.0) && (prefix < 70.0.154.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (44~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.152.0) && (prefix < 70.0.153.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (45~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (46~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.80.0) && (prefix < 70.0.81.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (47~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (48~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.81.0) && (prefix < 70.0.82.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (49~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (50~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.82.0) && (prefix < 70.0.83.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (51~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (52~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.83.0) && (prefix < 70.0.84.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (53~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~24) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~23) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~20) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~25) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~21) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~22) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (54~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (55~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.139.0) && (prefix < 70.0.140.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (56~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.138.0) && (prefix < 70.0.139.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (57~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~129) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~132) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~135) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~137) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~118) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~126) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (58~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (59~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.140.0) && (prefix < 70.0.141.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (60~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (61~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.142.0) && (prefix < 70.0.143.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (62~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.141.0) && (prefix < 70.0.142.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (63~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.90.0) && (prefix < 70.0.91.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (64~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.95.0) && (prefix < 70.0.96.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (65~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (66~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (67~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.91.0) && (prefix < 70.0.92.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (68~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (69~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.92.0) && (prefix < 70.0.93.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (70~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (71~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.93.0) && (prefix < 70.0.94.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~73) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~69) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~64) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~63) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~71) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (72~67) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (73~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.94.0) && (prefix < 70.0.95.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (74~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (75~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.66.0) && (prefix < 70.0.67.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (76~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (77~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.67.0) && (prefix < 70.0.68.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (78~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.68.0) && (prefix < 70.0.69.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (79~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.69.0) && (prefix < 70.0.70.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (80~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (81~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (82~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (83~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.166.0) && (prefix < 70.0.167.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (84~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.165.0) && (prefix < 70.0.166.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (85~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.167.0) && (prefix < 70.0.168.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (86~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.175.0) && (prefix < 70.0.176.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (87~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.174.0) && (prefix < 70.0.175.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (88~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (89~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (90~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (91~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (92~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.78.0) && (prefix < 70.0.79.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (93~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.79.0) && (prefix < 70.0.80.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (94~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (95~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (96~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (97~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (98~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (99~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (100~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (101~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.155.0) && (prefix < 70.0.156.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (102~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.154.0) && (prefix < 70.0.155.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (103~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.162.0) && (prefix < 70.0.163.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (104~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.164.0) && (prefix < 70.0.165.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (105~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.163.0) && (prefix < 70.0.164.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~168) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~166) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~167) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~87) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~164) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (106~86) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~16) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~150) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~18) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~65) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~36) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~0) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (107~96) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (108~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.70.0) && (prefix < 70.0.71.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~35) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (109~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.71.0) && (prefix < 70.0.72.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (110~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (111~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (112~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (113~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (114~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (115~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (116~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.44.0) && (prefix < 70.0.45.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (117~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (118~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.107.0) && (prefix < 70.0.108.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (119~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.45.0) && (prefix < 70.0.46.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (120~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (121~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.46.0) && (prefix < 70.0.47.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~12) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~29) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~54) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~9) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~165) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~60) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~30) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (122~136) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (123~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.47.0) && (prefix < 70.0.48.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (124~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (125~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (126~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.104.0) && (prefix < 70.0.105.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (127~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (128~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (129~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.103.0) && (prefix < 70.0.104.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (130~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (131~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (132~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.106.0) && (prefix < 70.0.107.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (133~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (134~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (135~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.105.0) && (prefix < 70.0.106.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~85) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~104) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~105) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~83) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~84) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~103) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (136~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~47) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~57) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (137~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.102.0) && (prefix < 70.0.103.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (138~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (139~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (140~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (141~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (142~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (143~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (144~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~152) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~37) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~98) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~1) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~58) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~19) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~70) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~17) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~33) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (145~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (146~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.42.0) && (prefix < 70.0.43.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~170) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~179) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~89) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (147~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.43.0) && (prefix < 70.0.44.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (148~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (149~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (150~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (151~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.59.0) && (prefix < 70.0.60.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (152~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~133) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~124) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~127) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~171) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~169) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (153~130) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~175) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~172) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~160) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~174) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~176) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~173) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (154~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (155~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.55.0) && (prefix < 70.0.56.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (156~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (157~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.56.0) && (prefix < 70.0.57.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (158~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.57.0) && (prefix < 70.0.58.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (159~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.58.0) && (prefix < 70.0.59.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (160~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (161~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~32) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~113) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~45) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~53) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~10) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~8) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~68) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~88) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~28) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (162~163) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~125) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~128) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~131) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~161) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~162) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~134) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (163~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (164~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.177.0) && (prefix < 70.0.178.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~101) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~44) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~102) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~41) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~43) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (165~42) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (166~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.176.0) && (prefix < 70.0.177.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (167~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.179.0) && (prefix < 70.0.180.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~91) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~81) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~80) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (168~90) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.178.0) && (prefix < 70.0.179.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (169~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~143) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~144) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~141) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~142) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~139) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (170~145) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~74) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~153) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~82) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~14) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~49) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~34) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~72) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~2) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~38) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~26) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (171~110) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (172~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (173~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (174~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (175~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~51) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~40) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~66) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~111) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~76) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~15) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~27) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~31) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~154) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~106) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (176~7) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~120) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~117) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~159) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~155) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~114) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~115) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~157) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~151) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~178) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~112) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~158) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (177~122) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~156) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~149) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~177) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~148) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~140) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (178~138) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if ((prefixLen = 24u6) && ((prefix >= 70.0.54.0) && (prefix < 70.0.55.0))) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~99) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~146) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~119) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~116) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~123) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~94) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~121) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~107) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~97) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~100) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~95) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)
   | (179~147) ->
   let b = x0.bgp in
      (match b with
      | None -> None
      | Some b ->
     let b = 
           if (isProtocol prot p_BGP) then
(Some {b with lp= 100; aslen= b.aslen + 1; })
 else
None
     in
      b)


let trans d edge x = 
  let o = transferOspf edge x.ospf in
  let b = transferBgp d edge x in
  {connected=None; static=None; ospf=o; bgp=b; selected=None}

let nodes = 180

let edges = {
  0-61; (*aggregation-137,Serial8 --> edge-142,Serial5*)
  19-139; (*aggregation-124,Serial9 --> core-29,Serial11*)
  58-143; (*aggregation-100,Serial8 --> core-26,Serial2*)
  163-43; (*aggregation-145,Serial9 --> edge-153,Serial1*)
  163-42; (*aggregation-145,Serial6 --> edge-150,Serial1*)
  162-8; (*core-10,Serial11 --> aggregation-133,Serial11*)
  54-117; (*aggregation-108,Serial2 --> core-2,Serial5*)
  14-171; (*aggregation-75,Serial1 --> core-18,Serial3*)
  115-47; (*core-3,Serial0 --> aggregation-96,Serial3*)
  75-30; (*edge-66,Serial2 --> aggregation-60,Serial2*)
  134-113; (*core-8,Serial5 --> aggregation-157,Serial6*)
  54-22; (*aggregation-108,Serial7 --> edge-115,Serial0*)
  54-120; (*aggregation-108,Serial5 --> core-5,Serial5*)
  149-144; (*aggregation-52,Serial3 --> core-25,Serial8*)
  98-142; (*aggregation-160,Serial9 --> core-27,Serial1*)
  76-175; (*aggregation-38,Serial9 --> core-14,Serial2*)
  61-7; (*edge-142,Serial2 --> aggregation-134,Serial5*)
  15-46; (*aggregation-74,Serial6 --> edge-80,Serial2*)
  80-168; (*aggregation-173,Serial8 --> edge-178,Serial5*)
  45-131; (*aggregation-97,Serial9 --> core-9,Serial0*)
  114-89; (*core-0,Serial2 --> aggregation-36,Serial0*)
  139-17; (*core-29,Serial5 --> aggregation-76,Serial11*)
  124-82; (*core-23,Serial7 --> aggregation-171,Serial11*)
  57-94; (*aggregation-101,Serial2 --> core-34,Serial1*)
  68-134; (*aggregation-85,Serial2 --> core-8,Serial7*)
  44-152; (*edge-152,Serial4 --> aggregation-148,Serial6*)
  120-165; (*core-5,Serial1 --> aggregation-144,Serial5*)
  8-128; (*aggregation-133,Serial1 --> core-6,Serial11*)
  67-72; (*edge-91,Serial3 --> aggregation-87,Serial7*)
  82-87; (*aggregation-171,Serial2 --> edge-174,Serial3*)
  82-164; (*aggregation-171,Serial5 --> edge-177,Serial3*)
  143-149; (*core-26,Serial8 --> aggregation-52,Serial11*)
  8-131; (*aggregation-133,Serial4 --> core-9,Serial11*)
  58-118; (*aggregation-100,Serial5 --> edge-107,Serial4*)
  127-138; (*core-22,Serial6 --> aggregation-51,Serial0*)
  80-95; (*aggregation-173,Serial1 --> core-33,Serial5*)
  172-76; (*core-17,Serial2 --> aggregation-38,Serial8*)
  98-145; (*aggregation-160,Serial6 --> core-24,Serial1*)
  131-32; (*core-9,Serial10 --> aggregation-61,Serial7*)
  28-134; (*aggregation-121,Serial6 --> core-8,Serial9*)
  36-97; (*aggregation-113,Serial0 --> core-32,Serial6*)
  175-154; (*core-14,Serial4 --> aggregation-146,Serial2*)
  119-74; (*edge-45,Serial3 --> aggregation-39,Serial3*)
  130-153; (*core-21,Serial1 --> aggregation-147,Serial5*)
  0-107; (*aggregation-137,Serial3 --> core-35,Serial8*)
  5-28; (*edge-128,Serial1 --> aggregation-121,Serial0*)
  15-52; (*aggregation-74,Serial9 --> edge-83,Serial2*)
  82-127; (*aggregation-171,Serial10 --> core-22,Serial7*)
  169-72; (*core-19,Serial8 --> aggregation-87,Serial1*)
  19-143; (*aggregation-124,Serial6 --> core-26,Serial11*)
  85-113; (*edge-167,Serial3 --> aggregation-157,Serial5*)
  173-27; (*core-16,Serial9 --> aggregation-122,Serial8*)
  51-173; (*aggregation-98,Serial10 --> core-16,Serial0*)
  105-96; (*edge-163,Serial1 --> aggregation-161,Serial3*)
  17-143; (*aggregation-76,Serial8 --> core-26,Serial5*)
  117-60; (*core-2,Serial8 --> aggregation-84,Serial2*)
  175-51; (*core-14,Serial0 --> aggregation-98,Serial8*)
  97-96; (*core-32,Serial2 --> aggregation-161,Serial0*)
  47-137; (*aggregation-96,Serial6 --> edge-102,Serial0*)
  1-142; (*aggregation-136,Serial9 --> core-27,Serial7*)
  171-153; (*core-18,Serial1 --> aggregation-147,Serial2*)
  0-97; (*aggregation-137,Serial0 --> core-32,Serial8*)
  32-108; (*aggregation-61,Serial4 --> edge-70,Serial3*)
  88-123; (*aggregation-37,Serial10 --> edge-47,Serial1*)
  43-153; (*edge-153,Serial3 --> aggregation-147,Serial9*)
  130-34; (*core-21,Serial11 --> aggregation-63,Serial9*)
  105-113; (*edge-163,Serial3 --> aggregation-157,Serial1*)
  5-19; (*edge-128,Serial4 --> aggregation-124,Serial0*)
  94-65; (*core-34,Serial9 --> aggregation-89,Serial2*)
  119-88; (*edge-45,Serial1 --> aggregation-37,Serial8*)
  152-102; (*aggregation-148,Serial8 --> edge-154,Serial4*)
  174-40; (*core-15,Serial5 --> aggregation-110,Serial3*)
  178-149; (*edge-54,Serial4 --> aggregation-52,Serial4*)
  70-139; (*aggregation-88,Serial2 --> core-29,Serial9*)
  16-94; (*aggregation-77,Serial2 --> core-34,Serial4*)
  152-41; (*aggregation-148,Serial5 --> edge-151,Serial4*)
  142-33; (*core-27,Serial0 --> aggregation-64,Serial9*)
  120-29; (*core-5,Serial9 --> aggregation-120,Serial5*)
  176-31; (*core-13,Serial11 --> aggregation-62,Serial7*)
  140-159; (*aggregation-50,Serial10 --> edge-58,Serial2*)
  79-32; (*edge-69,Serial3 --> aggregation-61,Serial3*)
  34-124; (*aggregation-63,Serial11 --> core-23,Serial11*)
  33-144; (*aggregation-64,Serial7 --> core-25,Serial0*)
  122-89; (*core-4,Serial2 --> aggregation-36,Serial4*)
  65-107; (*aggregation-89,Serial3 --> core-35,Serial9*)
  29-13; (*aggregation-120,Serial2 --> edge-130,Serial0*)
  103-111; (*edge-162,Serial4 --> aggregation-158,Serial0*)
  43-165; (*edge-153,Serial0 --> aggregation-144,Serial9*)
  0-100; (*aggregation-137,Serial10 --> core-30,Serial8*)
  80-86; (*aggregation-173,Serial5 --> edge-175,Serial5*)
  124-110; (*core-23,Serial4 --> aggregation-159,Serial11*)
  178-156; (*edge-54,Serial1 --> aggregation-49,Serial6*)
  116-76; (*edge-44,Serial2 --> aggregation-38,Serial3*)
  89-121; (*aggregation-36,Serial10 --> edge-46,Serial0*)
  156-157; (*aggregation-49,Serial8 --> edge-56,Serial1*)
  49-133; (*aggregation-99,Serial8 --> core-20,Serial0*)
  45-161; (*aggregation-97,Serial0 --> core-11,Serial0*)
  170-143; (*aggregation-40,Serial8 --> core-26,Serial3*)
  91-112; (*aggregation-168,Serial1 --> core-1,Serial7*)
  141-149; (*core-28,Serial8 --> aggregation-52,Serial10*)
  99-65; (*core-31,Serial9 --> aggregation-89,Serial4*)
  55-7; (*edge-139,Serial2 --> aggregation-134,Serial2*)
  35-108; (*aggregation-65,Serial6 --> edge-70,Serial1*)
  123-170; (*edge-47,Serial4 --> aggregation-40,Serial5*)
  52-17; (*edge-83,Serial4 --> aggregation-76,Serial5*)
  80-99; (*aggregation-173,Serial11 --> core-31,Serial5*)
  168-81; (*edge-178,Serial4 --> aggregation-172,Serial4*)
  81-139; (*aggregation-172,Serial11 --> core-29,Serial4*)
  126-57; (*edge-104,Serial5 --> aggregation-101,Serial6*)
  134-10; (*core-8,Serial3 --> aggregation-73,Serial2*)
  130-38; (*core-21,Serial5 --> aggregation-111,Serial11*)
  171-110; (*core-18,Serial4 --> aggregation-159,Serial6*)
  49-126; (*aggregation-99,Serial2 --> edge-104,Serial3*)
  177-120; (*aggregation-48,Serial5 --> core-5,Serial6*)
  59-0; (*edge-140,Serial5 --> aggregation-137,Serial6*)
  167-82; (*edge-179,Serial3 --> aggregation-171,Serial7*)
  65-97; (*aggregation-89,Serial0 --> core-32,Serial9*)
  0-55; (*aggregation-137,Serial5 --> edge-139,Serial5*)
  159-177; (*edge-58,Serial0 --> aggregation-48,Serial10*)
  4-26; (*edge-126,Serial3 --> aggregation-123,Serial10*)
  53-24; (*aggregation-109,Serial9 --> edge-117,Serial1*)
  2-124; (*aggregation-135,Serial11 --> core-23,Serial10*)
  44-163; (*edge-152,Serial1 --> aggregation-145,Serial8*)
  176-140; (*core-13,Serial6 --> aggregation-50,Serial1*)
  36-100; (*aggregation-113,Serial10 --> core-30,Serial6*)
  171-26; (*core-18,Serial9 --> aggregation-123,Serial4*)
  151-140; (*edge-59,Serial2 --> aggregation-50,Serial11*)
  102-163; (*edge-154,Serial1 --> aggregation-145,Serial10*)
  141-37; (*core-28,Serial6 --> aggregation-112,Serial10*)
  110-171; (*aggregation-159,Serial6 --> core-18,Serial4*)
  131-90; (*core-9,Serial8 --> aggregation-169,Serial3*)
  140-173; (*aggregation-50,Serial4 --> core-16,Serial6*)
  132-49; (*edge-106,Serial3 --> aggregation-99,Serial4*)
  83-111; (*edge-166,Serial4 --> aggregation-158,Serial4*)
  137-58; (*edge-102,Serial4 --> aggregation-100,Serial0*)
  144-81; (*core-25,Serial4 --> aggregation-172,Serial7*)
  3-29; (*edge-129,Serial0 --> aggregation-120,Serial1*)
  40-20; (*aggregation-110,Serial11 --> edge-119,Serial2*)
  64-66; (*edge-95,Serial2 --> aggregation-86,Serial11*)
  41-150; (*edge-151,Serial5 --> aggregation-149,Serial5*)
  79-35; (*edge-69,Serial1 --> aggregation-65,Serial5*)
  134-8; (*core-8,Serial11 --> aggregation-133,Serial3*)
  29-122; (*aggregation-120,Serial4 --> core-4,Serial9*)
  69-65; (*edge-92,Serial5 --> aggregation-89,Serial8*)
  11-19; (*edge-131,Serial4 --> aggregation-124,Serial3*)
  38-22; (*aggregation-111,Serial5 --> edge-115,Serial3*)
  128-88; (*core-6,Serial1 --> aggregation-37,Serial1*)
  90-167; (*aggregation-169,Serial11 --> edge-179,Serial1*)
  75-34; (*edge-66,Serial5 --> aggregation-63,Serial0*)
  81-144; (*aggregation-172,Serial7 --> core-25,Serial4*)
  1-56; (*aggregation-136,Serial0 --> edge-138,Serial4*)
  24-38; (*edge-117,Serial3 --> aggregation-111,Serial7*)
  154-175; (*aggregation-146,Serial2 --> core-14,Serial4*)
  12-114; (*aggregation-72,Serial0 --> core-0,Serial3*)
  88-147; (*aggregation-37,Serial6 --> edge-43,Serial1*)
  20-36; (*edge-119,Serial5 --> aggregation-113,Serial9*)
  26-4; (*aggregation-123,Serial10 --> edge-126,Serial3*)
  47-135; (*aggregation-96,Serial9 --> edge-105,Serial0*)
  125-156; (*core-7,Serial6 --> aggregation-49,Serial1*)
  150-97; (*aggregation-149,Serial0 --> core-32,Serial11*)
  150-100; (*aggregation-149,Serial10 --> core-30,Serial11*)
  1-145; (*aggregation-136,Serial6 --> core-24,Serial7*)
  97-179; (*core-32,Serial3 --> aggregation-41,Serial0*)
  172-66; (*core-17,Serial8 --> aggregation-86,Serial5*)
  152-143; (*aggregation-148,Serial11 --> core-26,Serial10*)
  136-117; (*aggregation-156,Serial8 --> core-2,Serial4*)
  101-150; (*edge-155,Serial5 --> aggregation-149,Serial9*)
  63-68; (*edge-90,Serial1 --> aggregation-85,Serial6*)
  152-139; (*aggregation-148,Serial1 --> core-29,Serial10*)
  179-116; (*aggregation-41,Serial6 --> edge-44,Serial5*)
  153-171; (*aggregation-147,Serial2 --> core-18,Serial1*)
  160-76; (*core-12,Serial2 --> aggregation-38,Serial0*)
  87-90; (*edge-174,Serial1 --> aggregation-169,Serial6*)
  104-136; (*edge-164,Serial2 --> aggregation-156,Serial4*)
  12-50; (*aggregation-72,Serial10 --> edge-82,Serial0*)
  82-171; (*aggregation-171,Serial1 --> core-18,Serial7*)
  34-79; (*aggregation-63,Serial3 --> edge-69,Serial5*)
  35-100; (*aggregation-65,Serial10 --> core-30,Serial0*)
  148-159; (*aggregation-53,Serial8 --> edge-58,Serial5*)
  45-137; (*aggregation-97,Serial2 --> edge-102,Serial1*)
  32-77; (*aggregation-61,Serial1 --> edge-67,Serial3*)
  155-138; (*edge-55,Serial3 --> aggregation-51,Serial7*)
  30-122; (*aggregation-60,Serial10 --> core-4,Serial11*)
  149-139; (*aggregation-52,Serial1 --> core-29,Serial8*)
  72-63; (*aggregation-87,Serial6 --> edge-90,Serial3*)
  165-41; (*aggregation-144,Serial7 --> edge-151,Serial0*)
  16-92; (*aggregation-77,Serial4 --> edge-78,Serial5*)
  22-36; (*edge-115,Serial5 --> aggregation-113,Serial5*)
  107-36; (*core-35,Serial6 --> aggregation-113,Serial3*)
  55-1; (*edge-139,Serial4 --> aggregation-136,Serial1*)
  60-112; (*aggregation-84,Serial1 --> core-1,Serial8*)
  58-126; (*aggregation-100,Serial2 --> edge-104,Serial4*)
  149-143; (*aggregation-52,Serial11 --> core-26,Serial8*)
  26-5; (*aggregation-123,Serial0 --> edge-128,Serial3*)
  179-99; (*aggregation-41,Serial11 --> core-31,Serial3*)
  26-127; (*aggregation-123,Serial8 --> core-22,Serial9*)
  53-162; (*aggregation-109,Serial4 --> core-10,Serial4*)
  149-157; (*aggregation-52,Serial6 --> edge-56,Serial4*)
  53-128; (*aggregation-109,Serial0 --> core-6,Serial4*)
  136-83; (*aggregation-156,Serial6 --> edge-166,Serial2*)
  139-170; (*core-29,Serial3 --> aggregation-40,Serial11*)
  153-101; (*aggregation-147,Serial11 --> edge-155,Serial3*)
  123-89; (*edge-47,Serial0 --> aggregation-36,Serial11*)
  9-39; (*aggregation-132,Serial11 --> edge-143,Serial0*)
  10-46; (*aggregation-73,Serial8 --> edge-80,Serial1*)
  162-68; (*core-10,Serial7 --> aggregation-85,Serial4*)
  172-140; (*core-17,Serial6 --> aggregation-50,Serial5*)
  47-120; (*aggregation-96,Serial5 --> core-5,Serial0*)
  97-80; (*core-32,Serial5 --> aggregation-173,Serial0*)
  171-38; (*core-18,Serial5 --> aggregation-111,Serial3*)
  70-69; (*aggregation-88,Serial8 --> edge-92,Serial4*)
  120-91; (*core-5,Serial7 --> aggregation-168,Serial5*)
  23-38; (*edge-114,Serial3 --> aggregation-111,Serial4*)
  25-53; (*edge-116,Serial1 --> aggregation-109,Serial8*)
  143-152; (*core-26,Serial10 --> aggregation-148,Serial11*)
  90-134; (*aggregation-169,Serial2 --> core-8,Serial8*)
  68-69; (*aggregation-85,Serial8 --> edge-92,Serial1*)
  29-115; (*aggregation-120,Serial9 --> core-3,Serial9*)
  27-175; (*aggregation-122,Serial6 --> core-14,Serial9*)
  29-114; (*aggregation-120,Serial6 --> core-0,Serial9*)
  33-109; (*aggregation-64,Serial5 --> edge-71,Serial0*)
  130-14; (*core-21,Serial3 --> aggregation-75,Serial9*)
  165-102; (*aggregation-144,Serial10 --> edge-154,Serial0*)
  76-174; (*aggregation-38,Serial10 --> core-15,Serial2*)
  177-178; (*aggregation-48,Serial6 --> edge-54,Serial0*)
  30-112; (*aggregation-60,Serial1 --> core-1,Serial11*)
  171-138; (*core-18,Serial6 --> aggregation-51,Serial2*)
  177-158; (*aggregation-48,Serial9 --> edge-57,Serial0*)
  40-25; (*aggregation-110,Serial8 --> edge-116,Serial2*)
  41-154; (*edge-151,Serial2 --> aggregation-146,Serial7*)
  10-162; (*aggregation-73,Serial4 --> core-10,Serial3*)
  137-49; (*edge-102,Serial3 --> aggregation-99,Serial0*)
  2-55; (*aggregation-135,Serial1 --> edge-139,Serial3*)
  158-148; (*edge-57,Serial5 --> aggregation-53,Serial7*)
  133-82; (*core-20,Serial7 --> aggregation-171,Serial8*)
  8-56; (*aggregation-133,Serial5 --> edge-138,Serial1*)
  136-105; (*aggregation-156,Serial3 --> edge-163,Serial2*)
  125-113; (*core-7,Serial5 --> aggregation-157,Serial9*)
  112-91; (*core-1,Serial7 --> aggregation-168,Serial1*)
  99-150; (*core-31,Serial11 --> aggregation-149,Serial11*)
  179-95; (*aggregation-41,Serial1 --> core-33,Serial3*)
  71-70; (*edge-93,Serial4 --> aggregation-88,Serial9*)
  95-150; (*core-33,Serial11 --> aggregation-149,Serial1*)
  115-54; (*core-3,Serial5 --> aggregation-108,Serial3*)
  10-134; (*aggregation-73,Serial2 --> core-8,Serial3*)
  131-88; (*core-9,Serial1 --> aggregation-37,Serial4*)
  13-27; (*edge-130,Serial2 --> aggregation-122,Serial2*)
  153-41; (*aggregation-147,Serial7 --> edge-151,Serial3*)
  130-110; (*core-21,Serial4 --> aggregation-159,Serial9*)
  142-17; (*core-27,Serial5 --> aggregation-76,Serial9*)
  20-53; (*edge-119,Serial1 --> aggregation-109,Serial11*)
  95-0; (*core-33,Serial8 --> aggregation-137,Serial1*)
  135-58; (*edge-105,Serial4 --> aggregation-100,Serial3*)
  136-120; (*aggregation-156,Serial11 --> core-5,Serial4*)
  164-80; (*edge-177,Serial5 --> aggregation-173,Serial7*)
  148-100; (*aggregation-53,Serial10 --> core-30,Serial7*)
  87-81; (*edge-174,Serial4 --> aggregation-172,Serial0*)
  13-18; (*edge-130,Serial5 --> aggregation-125,Serial2*)
  1-141; (*aggregation-136,Serial10 --> core-28,Serial7*)
  164-106; (*edge-177,Serial2 --> aggregation-170,Serial7*)
  123-88; (*edge-47,Serial1 --> aggregation-37,Serial10*)
  8-59; (*aggregation-133,Serial7 --> edge-140,Serial1*)
  57-135; (*aggregation-101,Serial7 --> edge-105,Serial5*)
  77-31; (*edge-67,Serial4 --> aggregation-62,Serial1*)
  106-164; (*aggregation-170,Serial7 --> edge-177,Serial2*)
  89-115; (*aggregation-36,Serial3 --> core-3,Serial2*)
  170-147; (*aggregation-40,Serial1 --> edge-43,Serial4*)
  66-173; (*aggregation-86,Serial4 --> core-16,Serial8*)
  170-121; (*aggregation-40,Serial4 --> edge-46,Serial4*)
  66-176; (*aggregation-86,Serial1 --> core-13,Serial8*)
  101-165; (*edge-155,Serial0 --> aggregation-144,Serial11*)
  121-179; (*edge-46,Serial5 --> aggregation-41,Serial8*)
  115-9; (*core-3,Serial10 --> aggregation-132,Serial3*)
  176-66; (*core-13,Serial8 --> aggregation-86,Serial1*)
  73-60; (*edge-94,Serial0 --> aggregation-84,Serial10*)
  86-82; (*edge-175,Serial3 --> aggregation-171,Serial3*)
  75-33; (*edge-66,Serial0 --> aggregation-64,Serial0*)
  14-46; (*aggregation-75,Serial4 --> edge-80,Serial3*)
  1-62; (*aggregation-136,Serial3 --> edge-141,Serial4*)
  37-21; (*aggregation-112,Serial4 --> edge-118,Serial4*)
  135-47; (*edge-105,Serial0 --> aggregation-96,Serial9*)
  100-148; (*core-30,Serial7 --> aggregation-53,Serial10*)
  147-170; (*edge-43,Serial4 --> aggregation-40,Serial1*)
  97-57; (*core-32,Serial1 --> aggregation-101,Serial0*)
  111-175; (*aggregation-158,Serial8 --> core-14,Serial7*)
  162-28; (*core-10,Serial9 --> aggregation-121,Serial8*)
  171-14; (*core-18,Serial3 --> aggregation-75,Serial1*)
  88-134; (*aggregation-37,Serial3 --> core-8,Serial1*)
  26-11; (*aggregation-123,Serial3 --> edge-131,Serial3*)
  84-110; (*edge-165,Serial5 --> aggregation-159,Serial3*)
  108-31; (*edge-70,Serial4 --> aggregation-62,Serial4*)
  92-15; (*edge-78,Serial2 --> aggregation-74,Serial4*)
  143-19; (*core-26,Serial11 --> aggregation-124,Serial6*)
  144-1; (*core-25,Serial7 --> aggregation-136,Serial7*)
  165-117; (*aggregation-144,Serial2 --> core-2,Serial1*)
  153-130; (*aggregation-147,Serial5 --> core-21,Serial1*)
  30-78; (*aggregation-60,Serial4 --> edge-68,Serial2*)
  111-104; (*aggregation-158,Serial2 --> edge-164,Serial4*)
  98-105; (*aggregation-160,Serial1 --> edge-163,Serial0*)
  173-51; (*core-16,Serial0 --> aggregation-98,Serial10*)
  139-33; (*core-29,Serial0 --> aggregation-64,Serial11*)
  93-16; (*edge-79,Serial5 --> aggregation-77,Serial5*)
  146-179; (*edge-42,Serial5 --> aggregation-41,Serial4*)
  156-151; (*aggregation-49,Serial11 --> edge-59,Serial1*)
  45-128; (*aggregation-97,Serial1 --> core-6,Serial0*)
  52-12; (*edge-83,Serial0 --> aggregation-72,Serial11*)
  148-97; (*aggregation-53,Serial0 --> core-32,Serial7*)
  74-130; (*aggregation-39,Serial9 --> core-21,Serial2*)
  110-85; (*aggregation-159,Serial5 --> edge-167,Serial5*)
  118-51; (*edge-107,Serial2 --> aggregation-98,Serial5*)
  6-18; (*edge-127,Serial5 --> aggregation-125,Serial11*)
  176-111; (*core-13,Serial7 --> aggregation-158,Serial7*)
  72-133; (*aggregation-87,Serial2 --> core-20,Serial8*)
  51-137; (*aggregation-98,Serial0 --> edge-102,Serial2*)
  138-158; (*aggregation-51,Serial9 --> edge-57,Serial3*)
  76-121; (*aggregation-38,Serial5 --> edge-46,Serial2*)
  150-42; (*aggregation-149,Serial4 --> edge-150,Serial5*)
  131-163; (*core-9,Serial2 --> aggregation-145,Serial3*)
  48-15; (*edge-81,Serial2 --> aggregation-74,Serial7*)
  111-84; (*aggregation-158,Serial3 --> edge-165,Serial4*)
  10-92; (*aggregation-73,Serial6 --> edge-78,Serial1*)
  144-70; (*core-25,Serial9 --> aggregation-88,Serial5*)
  31-175; (*aggregation-62,Serial8 --> core-14,Serial11*)
  57-137; (*aggregation-101,Serial4 --> edge-102,Serial5*)
  156-161; (*aggregation-49,Serial5 --> core-11,Serial6*)
  35-77; (*aggregation-65,Serial3 --> edge-67,Serial1*)
  26-13; (*aggregation-123,Serial2 --> edge-130,Serial3*)
  88-125; (*aggregation-37,Serial2 --> core-7,Serial1*)
  82-169; (*aggregation-171,Serial0 --> core-19,Serial7*)
  169-34; (*core-19,Serial11 --> aggregation-63,Serial7*)
  106-87; (*aggregation-170,Serial4 --> edge-174,Serial2*)
  38-127; (*aggregation-111,Serial1 --> core-22,Serial5*)
  94-148; (*core-34,Serial7 --> aggregation-53,Serial2*)
  130-138; (*core-21,Serial6 --> aggregation-51,Serial5*)
  148-107; (*aggregation-53,Serial3 --> core-35,Serial7*)
  6-27; (*edge-127,Serial2 --> aggregation-122,Serial11*)
  54-20; (*aggregation-108,Serial11 --> edge-119,Serial0*)
  2-169; (*aggregation-135,Serial7 --> core-19,Serial10*)
  22-40; (*edge-115,Serial2 --> aggregation-110,Serial7*)
  4-29; (*edge-126,Serial0 --> aggregation-120,Serial10*)
  74-123; (*aggregation-39,Serial5 --> edge-47,Serial3*)
  7-56; (*aggregation-134,Serial1 --> edge-138,Serial2*)
  9-122; (*aggregation-132,Serial4 --> core-4,Serial10*)
  113-103; (*aggregation-157,Serial0 --> edge-162,Serial3*)
  50-16; (*edge-82,Serial5 --> aggregation-77,Serial8*)
  174-15; (*core-15,Serial3 --> aggregation-74,Serial11*)
  127-153; (*core-22,Serial1 --> aggregation-147,Serial0*)
  134-53; (*core-8,Serial4 --> aggregation-109,Serial2*)
  150-43; (*aggregation-149,Serial7 --> edge-153,Serial5*)
  128-68; (*core-6,Serial7 --> aggregation-85,Serial0*)
  120-60; (*core-5,Serial8 --> aggregation-84,Serial5*)
  112-165; (*core-1,Serial1 --> aggregation-144,Serial1*)
  8-62; (*aggregation-133,Serial8 --> edge-141,Serial1*)
  142-58; (*core-27,Serial2 --> aggregation-100,Serial9*)
  73-65; (*edge-94,Serial5 --> aggregation-89,Serial10*)
  51-118; (*aggregation-98,Serial5 --> edge-107,Serial2*)
  115-12; (*core-3,Serial3 --> aggregation-72,Serial3*)
  155-177; (*edge-55,Serial0 --> aggregation-48,Serial7*)
  83-96; (*edge-166,Serial1 --> aggregation-161,Serial6*)
  135-45; (*edge-105,Serial1 --> aggregation-97,Serial5*)
  149-142; (*aggregation-52,Serial0 --> core-27,Serial8*)
  42-165; (*edge-150,Serial0 --> aggregation-144,Serial6*)
  69-66; (*edge-92,Serial2 --> aggregation-86,Serial8*)
  130-2; (*core-21,Serial10 --> aggregation-135,Serial9*)
  35-78; (*aggregation-65,Serial4 --> edge-68,Serial1*)
  138-171; (*aggregation-51,Serial2 --> core-18,Serial6*)
  96-97; (*aggregation-161,Serial0 --> core-32,Serial2*)
  157-140; (*edge-56,Serial2 --> aggregation-50,Serial8*)
  176-27; (*core-13,Serial9 --> aggregation-122,Serial5*)
  47-117; (*aggregation-96,Serial2 --> core-2,Serial0*)
  153-44; (*aggregation-147,Serial8 --> edge-152,Serial3*)
  112-29; (*core-1,Serial9 --> aggregation-120,Serial7*)
  13-28; (*edge-130,Serial1 --> aggregation-121,Serial2*)
  158-149; (*edge-57,Serial4 --> aggregation-52,Serial7*)
  140-176; (*aggregation-50,Serial1 --> core-13,Serial6*)
  143-37; (*core-26,Serial6 --> aggregation-112,Serial8*)
  173-106; (*core-16,Serial10 --> aggregation-170,Serial0*)
  32-125; (*aggregation-61,Serial9 --> core-7,Serial10*)
  152-142; (*aggregation-148,Serial0 --> core-27,Serial10*)
  116-89; (*edge-44,Serial0 --> aggregation-36,Serial8*)
  94-16; (*core-34,Serial4 --> aggregation-77,Serial2*)
  60-71; (*aggregation-84,Serial9 --> edge-93,Serial0*)
  166-91; (*edge-176,Serial0 --> aggregation-168,Serial8*)
  162-32; (*core-10,Serial10 --> aggregation-61,Serial8*)
  68-64; (*aggregation-85,Serial11 --> edge-95,Serial1*)
  153-127; (*aggregation-147,Serial0 --> core-22,Serial1*)
  129-51; (*edge-103,Serial2 --> aggregation-98,Serial1*)
  144-98; (*core-25,Serial1 --> aggregation-160,Serial7*)
  57-132; (*aggregation-101,Serial8 --> edge-106,Serial5*)
  27-3; (*aggregation-122,Serial1 --> edge-129,Serial2*)
  173-111; (*core-16,Serial7 --> aggregation-158,Serial10*)
  141-1; (*core-28,Serial7 --> aggregation-136,Serial10*)
  170-146; (*aggregation-40,Serial0 --> edge-42,Serial4*)
  46-12; (*edge-80,Serial0 --> aggregation-72,Serial8*)
  8-161; (*aggregation-133,Serial0 --> core-11,Serial11*)
  65-64; (*aggregation-89,Serial11 --> edge-95,Serial5*)
  112-177; (*core-1,Serial6 --> aggregation-48,Serial1*)
  179-97; (*aggregation-41,Serial0 --> core-32,Serial3*)
  32-78; (*aggregation-61,Serial2 --> edge-68,Serial3*)
  110-130; (*aggregation-159,Serial9 --> core-21,Serial4*)
  10-48; (*aggregation-73,Serial9 --> edge-81,Serial1*)
  65-67; (*aggregation-89,Serial7 --> edge-91,Serial5*)
  150-107; (*aggregation-149,Serial3 --> core-35,Serial11*)
  148-151; (*aggregation-53,Serial9 --> edge-59,Serial5*)
  61-2; (*edge-142,Serial3 --> aggregation-135,Serial4*)
  47-132; (*aggregation-96,Serial10 --> edge-106,Serial0*)
  152-144; (*aggregation-148,Serial3 --> core-25,Serial10*)
  27-6; (*aggregation-122,Serial11 --> edge-127,Serial2*)
  90-166; (*aggregation-169,Serial8 --> edge-176,Serial1*)
  130-72; (*core-21,Serial8 --> aggregation-87,Serial3*)
  9-59; (*aggregation-132,Serial8 --> edge-140,Serial0*)
  84-98; (*edge-165,Serial0 --> aggregation-160,Serial3*)
  113-128; (*aggregation-157,Serial11 --> core-6,Serial5*)
  48-14; (*edge-81,Serial3 --> aggregation-75,Serial5*)
  74-171; (*aggregation-39,Serial6 --> core-18,Serial2*)
  120-177; (*core-5,Serial6 --> aggregation-48,Serial5*)
  67-60; (*edge-91,Serial0 --> aggregation-84,Serial7*)
  148-155; (*aggregation-53,Serial5 --> edge-55,Serial5*)
  60-122; (*aggregation-84,Serial4 --> core-4,Serial8*)
  68-67; (*aggregation-85,Serial7 --> edge-91,Serial1*)
  91-87; (*aggregation-168,Serial6 --> edge-174,Serial0*)
  92-14; (*edge-78,Serial3 --> aggregation-75,Serial2*)
  70-71; (*aggregation-88,Serial9 --> edge-93,Serial4*)
  96-100; (*aggregation-161,Serial10 --> core-30,Serial2*)
  177-117; (*aggregation-48,Serial2 --> core-2,Serial6*)
  23-54; (*edge-114,Serial0 --> aggregation-108,Serial6*)
  31-75; (*aggregation-62,Serial0 --> edge-66,Serial4*)
  81-164; (*aggregation-172,Serial3 --> edge-177,Serial4*)
  96-104; (*aggregation-161,Serial4 --> edge-164,Serial1*)
  117-30; (*core-2,Serial11 --> aggregation-60,Serial8*)
  134-45; (*core-8,Serial0 --> aggregation-97,Serial8*)
  113-161; (*aggregation-157,Serial10 --> core-11,Serial5*)
  156-134; (*aggregation-49,Serial2 --> core-8,Serial6*)
  122-29; (*core-4,Serial9 --> aggregation-120,Serial4*)
  74-119; (*aggregation-39,Serial3 --> edge-45,Serial3*)
  174-154; (*core-15,Serial4 --> aggregation-146,Serial3*)
  141-17; (*core-28,Serial5 --> aggregation-76,Serial10*)
  38-25; (*aggregation-111,Serial6 --> edge-116,Serial3*)
  150-41; (*aggregation-149,Serial5 --> edge-151,Serial5*)
  116-88; (*edge-44,Serial1 --> aggregation-37,Serial7*)
  112-89; (*core-1,Serial2 --> aggregation-36,Serial1*)
  38-20; (*aggregation-111,Serial9 --> edge-119,Serial3*)
  65-100; (*aggregation-89,Serial5 --> core-30,Serial9*)
  15-160; (*aggregation-74,Serial2 --> core-12,Serial3*)
  113-83; (*aggregation-157,Serial4 --> edge-166,Serial3*)
  113-105; (*aggregation-157,Serial1 --> edge-163,Serial3*)
  63-60; (*edge-90,Serial0 --> aggregation-84,Serial6*)
  113-125; (*aggregation-157,Serial9 --> core-7,Serial5*)
  117-47; (*core-2,Serial0 --> aggregation-96,Serial2*)
  4-19; (*edge-126,Serial4 --> aggregation-124,Serial10*)
  140-155; (*aggregation-50,Serial7 --> edge-55,Serial2*)
  64-70; (*edge-95,Serial4 --> aggregation-88,Serial11*)
  102-154; (*edge-154,Serial2 --> aggregation-146,Serial10*)
  49-124; (*aggregation-99,Serial11 --> core-23,Serial0*)
  77-30; (*edge-67,Serial2 --> aggregation-60,Serial3*)
  131-113; (*core-9,Serial5 --> aggregation-157,Serial7*)
  9-55; (*aggregation-132,Serial7 --> edge-139,Serial0*)
  75-32; (*edge-66,Serial3 --> aggregation-61,Serial0*)
  170-139; (*aggregation-40,Serial11 --> core-29,Serial3*)
  148-95; (*aggregation-53,Serial1 --> core-33,Serial7*)
  150-99; (*aggregation-149,Serial11 --> core-31,Serial11*)
  55-8; (*edge-139,Serial1 --> aggregation-133,Serial6*)
  43-152; (*edge-153,Serial4 --> aggregation-148,Serial7*)
  122-136; (*core-4,Serial4 --> aggregation-156,Serial10*)
  81-143; (*aggregation-172,Serial8 --> core-26,Serial4*)
  15-92; (*aggregation-74,Serial4 --> edge-78,Serial2*)
  88-116; (*aggregation-37,Serial7 --> edge-44,Serial1*)
  37-20; (*aggregation-112,Serial5 --> edge-119,Serial4*)
  50-10; (*edge-82,Serial1 --> aggregation-73,Serial10*)
  111-174; (*aggregation-158,Serial9 --> core-15,Serial7*)
  91-120; (*aggregation-168,Serial5 --> core-5,Serial7*)
  154-160; (*aggregation-146,Serial0 --> core-12,Serial4*)
  32-134; (*aggregation-61,Serial6 --> core-8,Serial10*)
  121-74; (*edge-46,Serial3 --> aggregation-39,Serial4*)
  142-170; (*core-27,Serial3 --> aggregation-40,Serial9*)
  91-117; (*aggregation-168,Serial2 --> core-2,Serial7*)
  100-96; (*core-30,Serial2 --> aggregation-161,Serial10*)
  145-33; (*core-24,Serial0 --> aggregation-64,Serial6*)
  154-42; (*aggregation-146,Serial6 --> edge-150,Serial2*)
  91-164; (*aggregation-168,Serial9 --> edge-177,Serial0*)
  175-7; (*core-14,Serial1 --> aggregation-134,Serial9*)
  165-122; (*aggregation-144,Serial4 --> core-4,Serial1*)
  98-84; (*aggregation-160,Serial3 --> edge-165,Serial0*)
  131-28; (*core-9,Serial9 --> aggregation-121,Serial7*)
  21-40; (*edge-118,Serial2 --> aggregation-110,Serial10*)
  66-174; (*aggregation-86,Serial3 --> core-15,Serial8*)
  85-136; (*edge-167,Serial2 --> aggregation-156,Serial7*)
  133-153; (*core-20,Serial1 --> aggregation-147,Serial4*)
  140-174; (*aggregation-50,Serial3 --> core-15,Serial6*)
  49-132; (*aggregation-99,Serial4 --> edge-106,Serial3*)
  127-110; (*core-22,Serial4 --> aggregation-159,Serial10*)
  3-19; (*edge-129,Serial4 --> aggregation-124,Serial1*)
  34-133; (*aggregation-63,Serial8 --> core-20,Serial11*)
  56-0; (*edge-138,Serial5 --> aggregation-137,Serial4*)
  28-162; (*aggregation-121,Serial8 --> core-10,Serial9*)
  10-131; (*aggregation-73,Serial3 --> core-9,Serial3*)
  65-95; (*aggregation-89,Serial1 --> core-33,Serial9*)
  137-45; (*edge-102,Serial1 --> aggregation-97,Serial2*)
  161-32; (*core-11,Serial10 --> aggregation-61,Serial10*)
  16-97; (*aggregation-77,Serial0 --> core-32,Serial4*)
  31-108; (*aggregation-62,Serial4 --> edge-70,Serial4*)
  74-127; (*aggregation-39,Serial10 --> core-22,Serial2*)
  78-35; (*edge-68,Serial1 --> aggregation-65,Serial4*)
  20-54; (*edge-119,Serial0 --> aggregation-108,Serial11*)
  7-175; (*aggregation-134,Serial9 --> core-14,Serial1*)
  79-34; (*edge-69,Serial5 --> aggregation-63,Serial3*)
  104-113; (*edge-164,Serial3 --> aggregation-157,Serial2*)
  160-31; (*core-12,Serial11 --> aggregation-62,Serial6*)
  161-156; (*core-11,Serial6 --> aggregation-49,Serial5*)
  103-96; (*edge-162,Serial1 --> aggregation-161,Serial2*)
  115-60; (*core-3,Serial8 --> aggregation-84,Serial3*)
  41-163; (*edge-151,Serial1 --> aggregation-145,Serial7*)
  89-147; (*aggregation-36,Serial7 --> edge-43,Serial0*)
  17-139; (*aggregation-76,Serial11 --> core-29,Serial5*)
  12-93; (*aggregation-72,Serial7 --> edge-79,Serial0*)
  173-76; (*core-16,Serial2 --> aggregation-38,Serial7*)
  175-40; (*core-14,Serial5 --> aggregation-110,Serial2*)
  29-117; (*aggregation-120,Serial8 --> core-2,Serial9*)
  62-7; (*edge-141,Serial2 --> aggregation-134,Serial4*)
  106-86; (*aggregation-170,Serial5 --> edge-175,Serial2*)
  128-32; (*core-6,Serial10 --> aggregation-61,Serial11*)
  81-141; (*aggregation-172,Serial10 --> core-28,Serial4*)
  174-51; (*core-15,Serial0 --> aggregation-98,Serial9*)
  158-156; (*edge-57,Serial1 --> aggregation-49,Serial9*)
  120-89; (*core-5,Serial2 --> aggregation-36,Serial5*)
  54-21; (*aggregation-108,Serial10 --> edge-118,Serial0*)
  105-111; (*edge-163,Serial4 --> aggregation-158,Serial1*)
  170-145; (*aggregation-40,Serial6 --> core-24,Serial3*)
  16-46; (*aggregation-77,Serial6 --> edge-80,Serial5*)
  9-120; (*aggregation-132,Serial5 --> core-5,Serial10*)
  119-76; (*edge-45,Serial2 --> aggregation-38,Serial4*)
  17-145; (*aggregation-76,Serial6 --> core-24,Serial5*)
  167-81; (*edge-179,Serial4 --> aggregation-172,Serial5*)
  76-116; (*aggregation-38,Serial3 --> edge-44,Serial2*)
  97-18; (*core-32,Serial10 --> aggregation-125,Serial5*)
  141-98; (*core-28,Serial1 --> aggregation-160,Serial10*)
  94-18; (*core-34,Serial10 --> aggregation-125,Serial6*)
  34-127; (*aggregation-63,Serial10 --> core-22,Serial11*)
  179-146; (*aggregation-41,Serial4 --> edge-42,Serial5*)
  25-36; (*edge-116,Serial5 --> aggregation-113,Serial6*)
  6-26; (*edge-127,Serial3 --> aggregation-123,Serial11*)
  45-126; (*aggregation-97,Serial4 --> edge-104,Serial1*)
  44-150; (*edge-152,Serial5 --> aggregation-149,Serial6*)
  34-109; (*aggregation-63,Serial5 --> edge-71,Serial5*)
  0-94; (*aggregation-137,Serial2 --> core-34,Serial8*)
  18-11; (*aggregation-125,Serial3 --> edge-131,Serial5*)
  50-17; (*edge-82,Serial4 --> aggregation-76,Serial4*)
  23-37; (*edge-114,Serial4 --> aggregation-112,Serial0*)
  143-81; (*core-26,Serial4 --> aggregation-172,Serial8*)
  15-173; (*aggregation-74,Serial0 --> core-16,Serial3*)
  131-10; (*core-9,Serial3 --> aggregation-73,Serial3*)
  169-26; (*core-19,Serial9 --> aggregation-123,Serial5*)
  160-140; (*core-12,Serial6 --> aggregation-50,Serial0*)
  133-38; (*core-20,Serial5 --> aggregation-111,Serial10*)
  169-110; (*core-19,Serial4 --> aggregation-159,Serial7*)
  7-173; (*aggregation-134,Serial7 --> core-16,Serial1*)
  40-174; (*aggregation-110,Serial3 --> core-15,Serial5*)
  96-85; (*aggregation-161,Serial7 --> edge-167,Serial1*)
  57-129; (*aggregation-101,Serial5 --> edge-103,Serial5*)
  121-170; (*edge-46,Serial4 --> aggregation-40,Serial4*)
  152-101; (*aggregation-148,Serial9 --> edge-155,Serial4*)
  74-133; (*aggregation-39,Serial8 --> core-20,Serial2*)
  91-167; (*aggregation-168,Serial11 --> edge-179,Serial0*)
  106-160; (*aggregation-170,Serial2 --> core-12,Serial10*)
  134-156; (*core-8,Serial6 --> aggregation-49,Serial2*)
  109-35; (*edge-71,Serial1 --> aggregation-65,Serial7*)
  162-90; (*core-10,Serial8 --> aggregation-169,Serial4*)
  61-8; (*edge-142,Serial1 --> aggregation-133,Serial9*)
  169-74; (*core-19,Serial2 --> aggregation-39,Serial7*)
  33-78; (*aggregation-64,Serial2 --> edge-68,Serial0*)
  100-179; (*core-30,Serial3 --> aggregation-41,Serial10*)
  56-7; (*edge-138,Serial2 --> aggregation-134,Serial1*)
  135-49; (*edge-105,Serial3 --> aggregation-99,Serial3*)
  42-153; (*edge-150,Serial3 --> aggregation-147,Serial6*)
  164-90; (*edge-177,Serial1 --> aggregation-169,Serial9*)
  129-58; (*edge-103,Serial4 --> aggregation-100,Serial1*)
  167-80; (*edge-179,Serial5 --> aggregation-173,Serial9*)
  1-59; (*aggregation-136,Serial2 --> edge-140,Serial4*)
  125-8; (*core-7,Serial11 --> aggregation-133,Serial2*)
  95-35; (*core-33,Serial0 --> aggregation-65,Serial1*)
  134-90; (*core-8,Serial8 --> aggregation-169,Serial2*)
  67-65; (*edge-91,Serial5 --> aggregation-89,Serial7*)
  94-36; (*core-34,Serial6 --> aggregation-113,Serial2*)
  19-5; (*aggregation-124,Serial0 --> edge-128,Serial4*)
  35-97; (*aggregation-65,Serial0 --> core-32,Serial0*)
  179-107; (*aggregation-41,Serial3 --> core-35,Serial3*)
  149-145; (*aggregation-52,Serial2 --> core-24,Serial8*)
  47-115; (*aggregation-96,Serial3 --> core-3,Serial0*)
  146-74; (*edge-42,Serial3 --> aggregation-39,Serial0*)
  93-10; (*edge-79,Serial1 --> aggregation-73,Serial7*)
  96-84; (*aggregation-161,Serial5 --> edge-165,Serial1*)
  27-4; (*aggregation-122,Serial10 --> edge-126,Serial2*)
  107-80; (*core-35,Serial5 --> aggregation-173,Serial3*)
  157-138; (*edge-56,Serial3 --> aggregation-51,Serial8*)
  45-132; (*aggregation-97,Serial6 --> edge-106,Serial1*)
  40-22; (*aggregation-110,Serial7 --> edge-115,Serial2*)
  139-37; (*core-29,Serial6 --> aggregation-112,Serial11*)
  114-9; (*core-0,Serial10 --> aggregation-132,Serial0*)
  66-73; (*aggregation-86,Serial10 --> edge-94,Serial2*)
  69-72; (*edge-92,Serial3 --> aggregation-87,Serial8*)
  136-103; (*aggregation-156,Serial2 --> edge-162,Serial2*)
  2-61; (*aggregation-135,Serial4 --> edge-142,Serial3*)
  136-112; (*aggregation-156,Serial1 --> core-1,Serial4*)
  21-38; (*edge-118,Serial3 --> aggregation-111,Serial8*)
  87-80; (*edge-174,Serial5 --> aggregation-173,Serial4*)
  58-144; (*aggregation-100,Serial7 --> core-25,Serial2*)
  90-86; (*aggregation-169,Serial7 --> edge-175,Serial1*)
  149-178; (*aggregation-52,Serial4 --> edge-54,Serial4*)
  135-57; (*edge-105,Serial5 --> aggregation-101,Serial7*)
  105-136; (*edge-163,Serial2 --> aggregation-156,Serial3*)
  177-115; (*aggregation-48,Serial3 --> core-3,Serial6*)
  38-171; (*aggregation-111,Serial3 --> core-18,Serial5*)
  65-69; (*aggregation-89,Serial8 --> edge-92,Serial5*)
  29-5; (*aggregation-120,Serial0 --> edge-128,Serial0*)
  62-0; (*edge-141,Serial5 --> aggregation-137,Serial7*)
  176-15; (*core-13,Serial3 --> aggregation-74,Serial3*)
  14-169; (*aggregation-75,Serial0 --> core-19,Serial3*)
  138-130; (*aggregation-51,Serial5 --> core-21,Serial6*)
  154-43; (*aggregation-146,Serial9 --> edge-153,Serial2*)
  81-166; (*aggregation-172,Serial2 --> edge-176,Serial4*)
  121-89; (*edge-46,Serial0 --> aggregation-36,Serial10*)
  58-142; (*aggregation-100,Serial9 --> core-27,Serial2*)
  7-62; (*aggregation-134,Serial4 --> edge-141,Serial2*)
  173-140; (*core-16,Serial6 --> aggregation-50,Serial4*)
  108-30; (*edge-70,Serial2 --> aggregation-60,Serial6*)
  122-91; (*core-4,Serial7 --> aggregation-168,Serial4*)
  22-53; (*edge-115,Serial1 --> aggregation-109,Serial7*)
  58-137; (*aggregation-100,Serial0 --> edge-102,Serial4*)
  58-139; (*aggregation-100,Serial11 --> core-29,Serial2*)
  30-109; (*aggregation-60,Serial7 --> edge-71,Serial2*)
  138-127; (*aggregation-51,Serial0 --> core-22,Serial6*)
  66-63; (*aggregation-86,Serial6 --> edge-90,Serial2*)
  49-171; (*aggregation-99,Serial6 --> core-18,Serial0*)
  154-101; (*aggregation-146,Serial11 --> edge-155,Serial2*)
  71-68; (*edge-93,Serial1 --> aggregation-85,Serial9*)
  89-146; (*aggregation-36,Serial6 --> edge-42,Serial0*)
  95-148; (*core-33,Serial7 --> aggregation-53,Serial1*)
  128-163; (*core-6,Serial2 --> aggregation-145,Serial0*)
  16-48; (*aggregation-77,Serial7 --> edge-81,Serial5*)
  122-9; (*core-4,Serial10 --> aggregation-132,Serial4*)
  157-148; (*edge-56,Serial5 --> aggregation-53,Serial6*)
  12-92; (*aggregation-72,Serial6 --> edge-78,Serial0*)
  128-45; (*core-6,Serial0 --> aggregation-97,Serial1*)
  90-168; (*aggregation-169,Serial10 --> edge-178,Serial1*)
  132-58; (*edge-106,Serial4 --> aggregation-100,Serial4*)
  2-62; (*aggregation-135,Serial3 --> edge-141,Serial3*)
  37-143; (*aggregation-112,Serial8 --> core-26,Serial6*)
  9-61; (*aggregation-132,Serial10 --> edge-142,Serial0*)
  107-179; (*core-35,Serial3 --> aggregation-41,Serial3*)
  149-155; (*aggregation-52,Serial5 --> edge-55,Serial4*)
  166-106; (*edge-176,Serial2 --> aggregation-170,Serial6*)
  33-79; (*aggregation-64,Serial3 --> edge-69,Serial0*)
  72-73; (*aggregation-87,Serial10 --> edge-94,Serial3*)
  107-57; (*core-35,Serial1 --> aggregation-101,Serial3*)
  14-52; (*aggregation-75,Serial7 --> edge-83,Serial3*)
  94-0; (*core-34,Serial8 --> aggregation-137,Serial2*)
  94-150; (*core-34,Serial11 --> aggregation-149,Serial2*)
  124-26; (*core-23,Serial9 --> aggregation-123,Serial9*)
  141-70; (*core-28,Serial9 --> aggregation-88,Serial1*)
  81-87; (*aggregation-172,Serial0 --> edge-174,Serial4*)
  61-1; (*edge-142,Serial4 --> aggregation-136,Serial4*)
  18-6; (*aggregation-125,Serial11 --> edge-127,Serial5*)
  42-154; (*edge-150,Serial2 --> aggregation-146,Serial6*)
  47-129; (*aggregation-96,Serial7 --> edge-103,Serial0*)
  69-70; (*edge-92,Serial4 --> aggregation-88,Serial8*)
  1-55; (*aggregation-136,Serial1 --> edge-139,Serial4*)
  39-9; (*edge-143,Serial0 --> aggregation-132,Serial11*)
  79-33; (*edge-69,Serial0 --> aggregation-64,Serial3*)
  119-179; (*edge-45,Serial5 --> aggregation-41,Serial7*)
  36-94; (*aggregation-113,Serial2 --> core-34,Serial6*)
  30-117; (*aggregation-60,Serial8 --> core-2,Serial11*)
  166-82; (*edge-176,Serial3 --> aggregation-171,Serial4*)
  24-37; (*edge-117,Serial4 --> aggregation-112,Serial3*)
  40-173; (*aggregation-110,Serial4 --> core-16,Serial5*)
  179-147; (*aggregation-41,Serial5 --> edge-43,Serial5*)
  8-162; (*aggregation-133,Serial11 --> core-10,Serial11*)
  99-148; (*core-31,Serial7 --> aggregation-53,Serial11*)
  146-170; (*edge-42,Serial4 --> aggregation-40,Serial0*)
  114-54; (*core-0,Serial5 --> aggregation-108,Serial0*)
  96-94; (*aggregation-161,Serial8 --> core-34,Serial2*)
  112-136; (*core-1,Serial4 --> aggregation-156,Serial1*)
  91-115; (*aggregation-168,Serial3 --> core-3,Serial7*)
  81-145; (*aggregation-172,Serial6 --> core-24,Serial4*)
  21-53; (*edge-118,Serial1 --> aggregation-109,Serial10*)
  175-66; (*core-14,Serial8 --> aggregation-86,Serial2*)
  168-80; (*edge-178,Serial5 --> aggregation-173,Serial8*)
  11-18; (*edge-131,Serial5 --> aggregation-125,Serial3*)
  75-31; (*edge-66,Serial4 --> aggregation-62,Serial0*)
  85-111; (*edge-167,Serial4 --> aggregation-158,Serial5*)
  170-141; (*aggregation-40,Serial10 --> core-28,Serial3*)
  68-131; (*aggregation-85,Serial3 --> core-9,Serial7*)
  140-175; (*aggregation-50,Serial2 --> core-14,Serial6*)
  102-165; (*edge-154,Serial0 --> aggregation-144,Serial10*)
  140-178; (*aggregation-50,Serial6 --> edge-54,Serial2*)
  50-12; (*edge-82,Serial0 --> aggregation-72,Serial10*)
  92-16; (*edge-78,Serial5 --> aggregation-77,Serial4*)
  4-18; (*edge-126,Serial5 --> aggregation-125,Serial10*)
  86-81; (*edge-175,Serial4 --> aggregation-172,Serial1*)
  36-23; (*aggregation-113,Serial4 --> edge-114,Serial5*)
  171-34; (*core-18,Serial11 --> aggregation-63,Serial6*)
  114-29; (*core-0,Serial9 --> aggregation-120,Serial6*)
  51-174; (*aggregation-98,Serial9 --> core-15,Serial0*)
  143-1; (*core-26,Serial7 --> aggregation-136,Serial8*)
  19-145; (*aggregation-124,Serial4 --> core-24,Serial11*)
  0-62; (*aggregation-137,Serial7 --> edge-141,Serial5*)
  122-54; (*core-4,Serial5 --> aggregation-108,Serial4*)
  141-33; (*core-28,Serial0 --> aggregation-64,Serial10*)
  74-116; (*aggregation-39,Serial2 --> edge-44,Serial3*)
  113-84; (*aggregation-157,Serial3 --> edge-165,Serial3*)
  130-82; (*core-21,Serial7 --> aggregation-171,Serial9*)
  50-15; (*edge-82,Serial2 --> aggregation-74,Serial8*)
  83-110; (*edge-166,Serial5 --> aggregation-159,Serial4*)
  132-51; (*edge-106,Serial2 --> aggregation-98,Serial4*)
  137-47; (*edge-102,Serial0 --> aggregation-96,Serial6*)
  175-111; (*core-14,Serial7 --> aggregation-158,Serial8*)
  31-174; (*aggregation-62,Serial9 --> core-15,Serial11*)
  23-40; (*edge-114,Serial2 --> aggregation-110,Serial6*)
  111-103; (*aggregation-158,Serial0 --> edge-162,Serial4*)
  88-162; (*aggregation-37,Serial11 --> core-10,Serial1*)
  142-19; (*core-27,Serial11 --> aggregation-124,Serial7*)
  148-94; (*aggregation-53,Serial2 --> core-34,Serial7*)
  48-16; (*edge-81,Serial5 --> aggregation-77,Serial7*)
  80-164; (*aggregation-173,Serial7 --> edge-177,Serial5*)
  125-53; (*core-7,Serial4 --> aggregation-109,Serial1*)
  150-44; (*aggregation-149,Serial6 --> edge-152,Serial5*)
  113-104; (*aggregation-157,Serial2 --> edge-164,Serial3*)
  125-68; (*core-7,Serial7 --> aggregation-85,Serial1*)
  156-125; (*aggregation-49,Serial1 --> core-7,Serial6*)
  145-17; (*core-24,Serial5 --> aggregation-76,Serial6*)
  49-127; (*aggregation-99,Serial10 --> core-22,Serial0*)
  133-138; (*core-20,Serial6 --> aggregation-51,Serial4*)
  65-94; (*aggregation-89,Serial2 --> core-34,Serial9*)
  127-74; (*core-22,Serial2 --> aggregation-39,Serial10*)
  5-18; (*edge-128,Serial5 --> aggregation-125,Serial0*)
  14-130; (*aggregation-75,Serial9 --> core-21,Serial3*)
  178-177; (*edge-54,Serial0 --> aggregation-48,Serial6*)
  93-15; (*edge-79,Serial2 --> aggregation-74,Serial5*)
  133-49; (*core-20,Serial0 --> aggregation-99,Serial8*)
  170-144; (*aggregation-40,Serial7 --> core-25,Serial3*)
  152-43; (*aggregation-148,Serial7 --> edge-153,Serial4*)
  156-158; (*aggregation-49,Serial9 --> edge-57,Serial1*)
  17-93; (*aggregation-76,Serial1 --> edge-79,Serial4*)
  17-144; (*aggregation-76,Serial7 --> core-25,Serial5*)
  27-11; (*aggregation-122,Serial3 --> edge-131,Serial2*)
  74-124; (*aggregation-39,Serial11 --> core-23,Serial2*)
  76-147; (*aggregation-38,Serial2 --> edge-43,Serial2*)
  93-17; (*edge-79,Serial4 --> aggregation-76,Serial1*)
  172-40; (*core-17,Serial5 --> aggregation-110,Serial5*)
  103-110; (*edge-162,Serial5 --> aggregation-159,Serial0*)
  138-133; (*aggregation-51,Serial4 --> core-20,Serial6*)
  27-13; (*aggregation-122,Serial2 --> edge-130,Serial2*)
  91-122; (*aggregation-168,Serial4 --> core-4,Serial7*)
  34-169; (*aggregation-63,Serial7 --> core-19,Serial11*)
  107-65; (*core-35,Serial9 --> aggregation-89,Serial3*)
  57-97; (*aggregation-101,Serial0 --> core-32,Serial1*)
  66-64; (*aggregation-86,Serial11 --> edge-95,Serial2*)
  124-49; (*core-23,Serial0 --> aggregation-99,Serial11*)
  30-115; (*aggregation-60,Serial9 --> core-3,Serial11*)
  89-120; (*aggregation-36,Serial5 --> core-5,Serial2*)
  154-44; (*aggregation-146,Serial8 --> edge-152,Serial2*)
  82-167; (*aggregation-171,Serial7 --> edge-179,Serial3*)
  147-89; (*edge-43,Serial0 --> aggregation-36,Serial7*)
  106-166; (*aggregation-170,Serial6 --> edge-176,Serial2*)
  88-146; (*aggregation-37,Serial5 --> edge-42,Serial1*)
  163-162; (*aggregation-145,Serial4 --> core-10,Serial2*)
  122-60; (*core-4,Serial8 --> aggregation-84,Serial4*)
  144-37; (*core-25,Serial6 --> aggregation-112,Serial7*)
  137-51; (*edge-102,Serial2 --> aggregation-98,Serial0*)
  14-50; (*aggregation-75,Serial6 --> edge-82,Serial3*)
  132-45; (*edge-106,Serial1 --> aggregation-97,Serial6*)
  175-27; (*core-14,Serial9 --> aggregation-122,Serial6*)
  158-140; (*edge-57,Serial2 --> aggregation-50,Serial9*)
  9-56; (*aggregation-132,Serial6 --> edge-138,Serial0*)
  152-141; (*aggregation-148,Serial10 --> core-28,Serial10*)
  174-76; (*core-15,Serial2 --> aggregation-38,Serial10*)
  106-175; (*aggregation-170,Serial10 --> core-14,Serial10*)
  117-177; (*core-2,Serial6 --> aggregation-48,Serial2*)
  179-94; (*aggregation-41,Serial2 --> core-34,Serial3*)
  19-144; (*aggregation-124,Serial5 --> core-25,Serial11*)
  8-55; (*aggregation-133,Serial6 --> edge-139,Serial1*)
  3-26; (*edge-129,Serial3 --> aggregation-123,Serial1*)
  39-2; (*edge-143,Serial3 --> aggregation-135,Serial5*)
  37-139; (*aggregation-112,Serial11 --> core-29,Serial6*)
  49-118; (*aggregation-99,Serial5 --> edge-107,Serial3*)
  107-16; (*core-35,Serial4 --> aggregation-77,Serial3*)
  145-98; (*core-24,Serial1 --> aggregation-160,Serial6*)
  173-31; (*core-16,Serial11 --> aggregation-62,Serial10*)
  176-76; (*core-13,Serial2 --> aggregation-38,Serial11*)
  40-21; (*aggregation-110,Serial10 --> edge-118,Serial2*)
  47-126; (*aggregation-96,Serial8 --> edge-104,Serial0*)
  154-176; (*aggregation-146,Serial1 --> core-13,Serial4*)
  117-165; (*core-2,Serial1 --> aggregation-144,Serial2*)
  143-170; (*core-26,Serial3 --> aggregation-40,Serial8*)
  128-28; (*core-6,Serial9 --> aggregation-121,Serial4*)
  114-12; (*core-0,Serial3 --> aggregation-72,Serial0*)
  8-39; (*aggregation-133,Serial10 --> edge-143,Serial1*)
  7-59; (*aggregation-134,Serial3 --> edge-140,Serial2*)
  81-86; (*aggregation-172,Serial1 --> edge-175,Serial4*)
  72-171; (*aggregation-87,Serial0 --> core-18,Serial8*)
  145-58; (*core-24,Serial2 --> aggregation-100,Serial6*)
  127-72; (*core-22,Serial8 --> aggregation-87,Serial4*)
  165-120; (*aggregation-144,Serial5 --> core-5,Serial1*)
  28-6; (*aggregation-121,Serial11 --> edge-127,Serial1*)
  153-102; (*aggregation-147,Serial10 --> edge-154,Serial3*)
  15-93; (*aggregation-74,Serial5 --> edge-79,Serial2*)
  98-83; (*aggregation-160,Serial4 --> edge-166,Serial0*)
  36-107; (*aggregation-113,Serial3 --> core-35,Serial6*)
  91-166; (*aggregation-168,Serial8 --> edge-176,Serial0*)
  68-71; (*aggregation-85,Serial9 --> edge-93,Serial1*)
  83-98; (*edge-166,Serial0 --> aggregation-160,Serial4*)
  38-21; (*aggregation-111,Serial8 --> edge-118,Serial3*)
  50-14; (*edge-82,Serial3 --> aggregation-75,Serial6*)
  37-144; (*aggregation-112,Serial7 --> core-25,Serial6*)
  172-111; (*core-17,Serial7 --> aggregation-158,Serial11*)
  76-172; (*aggregation-38,Serial8 --> core-17,Serial2*)
  139-1; (*core-29,Serial7 --> aggregation-136,Serial11*)
  177-122; (*aggregation-48,Serial4 --> core-4,Serial6*)
  107-96; (*core-35,Serial2 --> aggregation-161,Serial9*)
  122-12; (*core-4,Serial3 --> aggregation-72,Serial4*)
  47-122; (*aggregation-96,Serial4 --> core-4,Serial0*)
  96-83; (*aggregation-161,Serial6 --> edge-166,Serial1*)
  113-162; (*aggregation-157,Serial8 --> core-10,Serial5*)
  169-2; (*core-19,Serial10 --> aggregation-135,Serial7*)
  45-135; (*aggregation-97,Serial5 --> edge-105,Serial1*)
  40-23; (*aggregation-110,Serial6 --> edge-114,Serial2*)
  115-30; (*core-3,Serial11 --> aggregation-60,Serial9*)
  49-169; (*aggregation-99,Serial7 --> core-19,Serial0*)
  136-114; (*aggregation-156,Serial0 --> core-0,Serial4*)
  26-124; (*aggregation-123,Serial9 --> core-23,Serial9*)
  7-172; (*aggregation-134,Serial8 --> core-17,Serial1*)
  53-161; (*aggregation-109,Serial5 --> core-11,Serial4*)
  71-66; (*edge-93,Serial2 --> aggregation-86,Serial9*)
  27-160; (*aggregation-122,Serial4 --> core-12,Serial9*)
  64-65; (*edge-95,Serial5 --> aggregation-89,Serial11*)
  30-108; (*aggregation-60,Serial6 --> edge-70,Serial2*)
  93-14; (*edge-79,Serial3 --> aggregation-75,Serial3*)
  103-98; (*edge-162,Serial0 --> aggregation-160,Serial0*)
  90-87; (*aggregation-169,Serial6 --> edge-174,Serial1*)
  66-67; (*aggregation-86,Serial7 --> edge-91,Serial2*)
  26-3; (*aggregation-123,Serial1 --> edge-129,Serial3*)
  65-71; (*aggregation-89,Serial9 --> edge-93,Serial5*)
  60-117; (*aggregation-84,Serial2 --> core-2,Serial8*)
  2-133; (*aggregation-135,Serial8 --> core-20,Serial10*)
  22-54; (*edge-115,Serial0 --> aggregation-108,Serial7*)
  32-75; (*aggregation-61,Serial0 --> edge-66,Serial3*)
  109-34; (*edge-71,Serial5 --> aggregation-63,Serial5*)
  120-136; (*core-5,Serial4 --> aggregation-156,Serial11*)
  138-155; (*aggregation-51,Serial7 --> edge-55,Serial3*)
  72-127; (*aggregation-87,Serial4 --> core-22,Serial8*)
  52-10; (*edge-83,Serial1 --> aggregation-73,Serial11*)
  106-176; (*aggregation-170,Serial3 --> core-13,Serial10*)
  156-162; (*aggregation-49,Serial4 --> core-10,Serial6*)
  160-7; (*core-12,Serial1 --> aggregation-134,Serial0*)
  35-95; (*aggregation-65,Serial1 --> core-33,Serial0*)
  133-72; (*core-20,Serial8 --> aggregation-87,Serial2*)
  139-149; (*core-29,Serial8 --> aggregation-52,Serial1*)
  88-131; (*aggregation-37,Serial4 --> core-9,Serial1*)
  90-164; (*aggregation-169,Serial9 --> edge-177,Serial1*)
  102-152; (*edge-154,Serial4 --> aggregation-148,Serial8*)
  110-124; (*aggregation-159,Serial11 --> core-23,Serial4*)
  147-88; (*edge-43,Serial1 --> aggregation-37,Serial6*)
  9-62; (*aggregation-132,Serial9 --> edge-141,Serial0*)
  65-63; (*aggregation-89,Serial6 --> edge-90,Serial5*)
  15-172; (*aggregation-74,Serial1 --> core-17,Serial3*)
  18-95; (*aggregation-125,Serial4 --> core-33,Serial10*)
  28-4; (*aggregation-121,Serial10 --> edge-126,Serial1*)
  111-83; (*aggregation-158,Serial4 --> edge-166,Serial4*)
  32-162; (*aggregation-61,Serial8 --> core-10,Serial10*)
  160-51; (*core-12,Serial0 --> aggregation-98,Serial6*)
  114-30; (*core-0,Serial11 --> aggregation-60,Serial0*)
  47-118; (*aggregation-96,Serial11 --> edge-107,Serial0*)
  83-136; (*edge-166,Serial2 --> aggregation-156,Serial6*)
  77-35; (*edge-67,Serial1 --> aggregation-65,Serial3*)
  21-54; (*edge-118,Serial0 --> aggregation-108,Serial10*)
  147-74; (*edge-43,Serial3 --> aggregation-39,Serial1*)
  161-163; (*core-11,Serial2 --> aggregation-145,Serial5*)
  84-113; (*edge-165,Serial3 --> aggregation-157,Serial3*)
  127-14; (*core-22,Serial3 --> aggregation-75,Serial10*)
  173-154; (*core-16,Serial4 --> aggregation-146,Serial4*)
  73-68; (*edge-94,Serial1 --> aggregation-85,Serial10*)
  20-40; (*edge-119,Serial2 --> aggregation-110,Serial11*)
  134-28; (*core-8,Serial9 --> aggregation-121,Serial6*)
  114-177; (*core-0,Serial6 --> aggregation-48,Serial0*)
  57-118; (*aggregation-101,Serial9 --> edge-107,Serial5*)
  40-172; (*aggregation-110,Serial5 --> core-17,Serial5*)
  99-80; (*core-31,Serial5 --> aggregation-173,Serial11*)
  153-42; (*aggregation-147,Serial6 --> edge-150,Serial3*)
  51-132; (*aggregation-98,Serial4 --> edge-106,Serial2*)
  144-152; (*core-25,Serial10 --> aggregation-148,Serial3*)
  114-60; (*core-0,Serial8 --> aggregation-84,Serial0*)
  120-47; (*core-5,Serial0 --> aggregation-96,Serial5*)
  89-112; (*aggregation-36,Serial1 --> core-1,Serial2*)
  8-61; (*aggregation-133,Serial9 --> edge-142,Serial1*)
  138-169; (*aggregation-51,Serial3 --> core-19,Serial6*)
  35-75; (*aggregation-65,Serial2 --> edge-66,Serial1*)
  123-74; (*edge-47,Serial3 --> aggregation-39,Serial5*)
  160-154; (*core-12,Serial4 --> aggregation-146,Serial0*)
  164-91; (*edge-177,Serial0 --> aggregation-168,Serial9*)
  59-7; (*edge-140,Serial2 --> aggregation-134,Serial3*)
  121-76; (*edge-46,Serial2 --> aggregation-38,Serial5*)
  157-156; (*edge-56,Serial1 --> aggregation-49,Serial8*)
  10-93; (*aggregation-73,Serial7 --> edge-79,Serial1*)
  14-48; (*aggregation-75,Serial5 --> edge-81,Serial3*)
  42-163; (*edge-150,Serial1 --> aggregation-145,Serial6*)
  62-8; (*edge-141,Serial1 --> aggregation-133,Serial8*)
  29-112; (*aggregation-120,Serial7 --> core-1,Serial9*)
  51-129; (*aggregation-98,Serial1 --> edge-103,Serial2*)
  176-40; (*core-13,Serial5 --> aggregation-110,Serial1*)
  57-126; (*aggregation-101,Serial6 --> edge-104,Serial5*)
  158-177; (*edge-57,Serial0 --> aggregation-48,Serial9*)
  26-171; (*aggregation-123,Serial4 --> core-18,Serial9*)
  10-161; (*aggregation-73,Serial5 --> core-11,Serial3*)
  18-100; (*aggregation-125,Serial8 --> core-30,Serial10*)
  144-33; (*core-25,Serial0 --> aggregation-64,Serial7*)
  157-149; (*edge-56,Serial4 --> aggregation-52,Serial6*)
  97-65; (*core-32,Serial9 --> aggregation-89,Serial0*)
  6-19; (*edge-127,Serial4 --> aggregation-124,Serial11*)
  41-165; (*edge-151,Serial0 --> aggregation-144,Serial7*)
  162-156; (*core-10,Serial6 --> aggregation-49,Serial4*)
  77-32; (*edge-67,Serial3 --> aggregation-61,Serial1*)
  16-50; (*aggregation-77,Serial8 --> edge-82,Serial5*)
  35-79; (*aggregation-65,Serial5 --> edge-69,Serial1*)
  101-154; (*edge-155,Serial2 --> aggregation-146,Serial11*)
  112-47; (*core-1,Serial0 --> aggregation-96,Serial1*)
  130-74; (*core-21,Serial2 --> aggregation-39,Serial9*)
  34-108; (*aggregation-63,Serial4 --> edge-70,Serial5*)
  177-155; (*aggregation-48,Serial7 --> edge-55,Serial0*)
  117-89; (*core-2,Serial2 --> aggregation-36,Serial2*)
  17-92; (*aggregation-76,Serial0 --> edge-78,Serial4*)
  156-159; (*aggregation-49,Serial10 --> edge-58,Serial1*)
  89-116; (*aggregation-36,Serial8 --> edge-44,Serial0*)
  139-98; (*core-29,Serial1 --> aggregation-160,Serial11*)
  31-176; (*aggregation-62,Serial7 --> core-13,Serial11*)
  131-53; (*core-9,Serial4 --> aggregation-109,Serial3*)
  111-105; (*aggregation-158,Serial1 --> edge-163,Serial4*)
  88-128; (*aggregation-37,Serial1 --> core-6,Serial1*)
  84-96; (*edge-165,Serial1 --> aggregation-161,Serial5*)
  161-8; (*core-11,Serial11 --> aggregation-133,Serial0*)
  6-28; (*edge-127,Serial1 --> aggregation-121,Serial11*)
  34-130; (*aggregation-63,Serial9 --> core-21,Serial11*)
  59-9; (*edge-140,Serial0 --> aggregation-132,Serial8*)
  2-171; (*aggregation-135,Serial6 --> core-18,Serial10*)
  176-7; (*core-13,Serial1 --> aggregation-134,Serial11*)
  128-8; (*core-6,Serial11 --> aggregation-133,Serial1*)
  28-5; (*aggregation-121,Serial0 --> edge-128,Serial1*)
  160-15; (*core-12,Serial3 --> aggregation-74,Serial2*)
  17-141; (*aggregation-76,Serial10 --> core-28,Serial5*)
  60-115; (*aggregation-84,Serial3 --> core-3,Serial8*)
  36-22; (*aggregation-113,Serial5 --> edge-115,Serial5*)
  100-36; (*core-30,Serial6 --> aggregation-113,Serial10*)
  76-146; (*aggregation-38,Serial1 --> edge-42,Serial2*)
  76-119; (*aggregation-38,Serial4 --> edge-45,Serial2*)
  108-35; (*edge-70,Serial1 --> aggregation-65,Serial6*)
  109-30; (*edge-71,Serial2 --> aggregation-60,Serial7*)
  161-90; (*core-11,Serial8 --> aggregation-169,Serial5*)
  125-90; (*core-7,Serial8 --> aggregation-169,Serial1*)
  103-136; (*edge-162,Serial2 --> aggregation-156,Serial2*)
  37-23; (*aggregation-112,Serial0 --> edge-114,Serial4*)
  72-169; (*aggregation-87,Serial1 --> core-19,Serial8*)
  170-123; (*aggregation-40,Serial5 --> edge-47,Serial4*)
  10-125; (*aggregation-73,Serial1 --> core-7,Serial3*)
  20-38; (*edge-119,Serial3 --> aggregation-111,Serial9*)
  12-117; (*aggregation-72,Serial2 --> core-2,Serial3*)
  31-109; (*aggregation-62,Serial5 --> edge-71,Serial4*)
  131-156; (*core-9,Serial6 --> aggregation-49,Serial3*)
  91-86; (*aggregation-168,Serial7 --> edge-175,Serial0*)
  107-18; (*core-35,Serial10 --> aggregation-125,Serial7*)
  148-178; (*aggregation-53,Serial4 --> edge-54,Serial5*)
  35-107; (*aggregation-65,Serial9 --> core-35,Serial0*)
  104-111; (*edge-164,Serial4 --> aggregation-158,Serial2*)
  142-81; (*core-27,Serial4 --> aggregation-172,Serial9*)
  142-1; (*core-27,Serial7 --> aggregation-136,Serial9*)
  154-173; (*aggregation-146,Serial4 --> core-16,Serial4*)
  32-79; (*aggregation-61,Serial3 --> edge-69,Serial3*)
  140-157; (*aggregation-50,Serial8 --> edge-56,Serial2*)
  43-150; (*edge-153,Serial5 --> aggregation-149,Serial7*)
  63-65; (*edge-90,Serial5 --> aggregation-89,Serial6*)
  40-24; (*aggregation-110,Serial9 --> edge-117,Serial2*)
  98-85; (*aggregation-160,Serial5 --> edge-167,Serial0*)
  174-111; (*core-15,Serial7 --> aggregation-158,Serial9*)
  153-124; (*aggregation-147,Serial1 --> core-23,Serial1*)
  78-34; (*edge-68,Serial5 --> aggregation-63,Serial2*)
  127-34; (*core-22,Serial11 --> aggregation-63,Serial10*)
  166-90; (*edge-176,Serial1 --> aggregation-169,Serial8*)
  90-128; (*aggregation-169,Serial0 --> core-6,Serial8*)
  38-130; (*aggregation-111,Serial11 --> core-21,Serial5*)
  74-147; (*aggregation-39,Serial1 --> edge-43,Serial3*)
  33-108; (*aggregation-64,Serial4 --> edge-70,Serial0*)
  66-175; (*aggregation-86,Serial2 --> core-14,Serial8*)
  88-119; (*aggregation-37,Serial8 --> edge-45,Serial1*)
  16-99; (*aggregation-77,Serial11 --> core-31,Serial4*)
  2-59; (*aggregation-135,Serial2 --> edge-140,Serial3*)
  106-167; (*aggregation-170,Serial9 --> edge-179,Serial2*)
  41-153; (*edge-151,Serial3 --> aggregation-147,Serial7*)
  123-179; (*edge-47,Serial5 --> aggregation-41,Serial9*)
  55-0; (*edge-139,Serial5 --> aggregation-137,Serial5*)
  64-60; (*edge-95,Serial0 --> aggregation-84,Serial11*)
  18-4; (*aggregation-125,Serial10 --> edge-126,Serial5*)
  136-122; (*aggregation-156,Serial10 --> core-4,Serial4*)
  145-149; (*core-24,Serial8 --> aggregation-52,Serial2*)
  32-131; (*aggregation-61,Serial7 --> core-9,Serial10*)
  68-73; (*aggregation-85,Serial10 --> edge-94,Serial1*)
  61-0; (*edge-142,Serial5 --> aggregation-137,Serial8*)
  164-82; (*edge-177,Serial3 --> aggregation-171,Serial5*)
  1-61; (*aggregation-136,Serial4 --> edge-142,Serial4*)
  81-168; (*aggregation-172,Serial4 --> edge-178,Serial4*)
  89-122; (*aggregation-36,Serial4 --> core-4,Serial2*)
  163-101; (*aggregation-145,Serial11 --> edge-155,Serial1*)
  74-169; (*aggregation-39,Serial7 --> core-19,Serial2*)
  13-29; (*edge-130,Serial0 --> aggregation-120,Serial2*)
  174-140; (*core-15,Serial6 --> aggregation-50,Serial3*)
  112-9; (*core-1,Serial10 --> aggregation-132,Serial1*)
  171-74; (*core-18,Serial2 --> aggregation-39,Serial6*)
  37-145; (*aggregation-112,Serial6 --> core-24,Serial6*)
  71-72; (*edge-93,Serial3 --> aggregation-87,Serial9*)
  99-179; (*core-31,Serial3 --> aggregation-41,Serial11*)
  114-91; (*core-0,Serial7 --> aggregation-168,Serial0*)
  153-43; (*aggregation-147,Serial9 --> edge-153,Serial3*)
  119-170; (*edge-45,Serial4 --> aggregation-40,Serial3*)
  132-57; (*edge-106,Serial5 --> aggregation-101,Serial8*)
  154-41; (*aggregation-146,Serial7 --> edge-151,Serial2*)
  78-30; (*edge-68,Serial2 --> aggregation-60,Serial4*)
  22-37; (*edge-115,Serial4 --> aggregation-112,Serial1*)
  158-138; (*edge-57,Serial3 --> aggregation-51,Serial9*)
  38-24; (*aggregation-111,Serial7 --> edge-117,Serial3*)
  176-106; (*core-13,Serial10 --> aggregation-170,Serial3*)
  11-27; (*edge-131,Serial2 --> aggregation-122,Serial3*)
  18-5; (*aggregation-125,Serial0 --> edge-128,Serial5*)
  23-53; (*edge-114,Serial1 --> aggregation-109,Serial6*)
  96-105; (*aggregation-161,Serial3 --> edge-163,Serial1*)
  171-82; (*core-18,Serial7 --> aggregation-171,Serial1*)
  163-125; (*aggregation-145,Serial1 --> core-7,Serial2*)
  56-8; (*edge-138,Serial1 --> aggregation-133,Serial5*)
  95-18; (*core-33,Serial10 --> aggregation-125,Serial4*)
  136-104; (*aggregation-156,Serial4 --> edge-164,Serial2*)
  76-173; (*aggregation-38,Serial7 --> core-16,Serial2*)
  69-68; (*edge-92,Serial1 --> aggregation-85,Serial8*)
  92-10; (*edge-78,Serial1 --> aggregation-73,Serial6*)
  163-161; (*aggregation-145,Serial5 --> core-11,Serial2*)
  132-47; (*edge-106,Serial0 --> aggregation-96,Serial10*)
  66-69; (*aggregation-86,Serial8 --> edge-92,Serial2*)
  94-80; (*core-34,Serial5 --> aggregation-173,Serial2*)
  34-171; (*aggregation-63,Serial6 --> core-18,Serial11*)
  115-91; (*core-3,Serial7 --> aggregation-168,Serial3*)
  73-70; (*edge-94,Serial4 --> aggregation-88,Serial10*)
  172-7; (*core-17,Serial1 --> aggregation-134,Serial8*)
  133-14; (*core-20,Serial3 --> aggregation-75,Serial8*)
  27-176; (*aggregation-122,Serial5 --> core-13,Serial9*)
  106-172; (*aggregation-170,Serial1 --> core-17,Serial10*)
  12-46; (*aggregation-72,Serial8 --> edge-80,Serial0*)
  59-1; (*edge-140,Serial4 --> aggregation-136,Serial2*)
  68-63; (*aggregation-85,Serial6 --> edge-90,Serial1*)
  162-53; (*core-10,Serial4 --> aggregation-109,Serial4*)
  2-130; (*aggregation-135,Serial9 --> core-21,Serial10*)
  15-176; (*aggregation-74,Serial3 --> core-13,Serial3*)
  34-77; (*aggregation-63,Serial1 --> edge-67,Serial5*)
  17-48; (*aggregation-76,Serial3 --> edge-81,Serial4*)
  22-38; (*edge-115,Serial3 --> aggregation-111,Serial5*)
  24-36; (*edge-117,Serial5 --> aggregation-113,Serial7*)
  177-112; (*aggregation-48,Serial1 --> core-1,Serial6*)
  95-36; (*core-33,Serial6 --> aggregation-113,Serial1*)
  128-10; (*core-6,Serial3 --> aggregation-73,Serial0*)
  161-113; (*core-11,Serial5 --> aggregation-157,Serial10*)
  137-57; (*edge-102,Serial5 --> aggregation-101,Serial4*)
  174-66; (*core-15,Serial8 --> aggregation-86,Serial3*)
  78-33; (*edge-68,Serial0 --> aggregation-64,Serial2*)
  116-179; (*edge-44,Serial5 --> aggregation-41,Serial6*)
  48-10; (*edge-81,Serial1 --> aggregation-73,Serial9*)
  94-35; (*core-34,Serial0 --> aggregation-65,Serial8*)
  79-31; (*edge-69,Serial4 --> aggregation-62,Serial3*)
  46-16; (*edge-80,Serial5 --> aggregation-77,Serial6*)
  31-172; (*aggregation-62,Serial11 --> core-17,Serial11*)
  92-17; (*edge-78,Serial4 --> aggregation-76,Serial0*)
  94-57; (*core-34,Serial1 --> aggregation-101,Serial2*)
  19-6; (*aggregation-124,Serial11 --> edge-127,Serial4*)
  70-73; (*aggregation-88,Serial10 --> edge-94,Serial4*)
  115-136; (*core-3,Serial4 --> aggregation-156,Serial9*)
  105-98; (*edge-163,Serial0 --> aggregation-160,Serial1*)
  149-159; (*aggregation-52,Serial8 --> edge-58,Serial4*)
  16-93; (*aggregation-77,Serial5 --> edge-79,Serial5*)
  144-17; (*core-25,Serial5 --> aggregation-76,Serial7*)
  127-26; (*core-22,Serial9 --> aggregation-123,Serial8*)
  139-70; (*core-29,Serial9 --> aggregation-88,Serial2*)
  14-124; (*aggregation-75,Serial11 --> core-23,Serial3*)
  39-1; (*edge-143,Serial4 --> aggregation-136,Serial5*)
  174-7; (*core-15,Serial1 --> aggregation-134,Serial10*)
  16-95; (*aggregation-77,Serial1 --> core-33,Serial4*)
  29-3; (*aggregation-120,Serial1 --> edge-129,Serial0*)
  118-58; (*edge-107,Serial4 --> aggregation-100,Serial5*)
  86-80; (*edge-175,Serial5 --> aggregation-173,Serial5*)
  90-162; (*aggregation-169,Serial4 --> core-10,Serial8*)
  145-19; (*core-24,Serial11 --> aggregation-124,Serial4*)
  3-18; (*edge-129,Serial5 --> aggregation-125,Serial1*)
  106-174; (*aggregation-170,Serial11 --> core-15,Serial10*)
  107-0; (*core-35,Serial8 --> aggregation-137,Serial3*)
  47-112; (*aggregation-96,Serial1 --> core-1,Serial0*)
  120-9; (*core-5,Serial10 --> aggregation-132,Serial5*)
  152-145; (*aggregation-148,Serial2 --> core-24,Serial10*)
  120-30; (*core-5,Serial11 --> aggregation-60,Serial11*)
  94-179; (*core-34,Serial3 --> aggregation-41,Serial2*)
  142-98; (*core-27,Serial1 --> aggregation-160,Serial9*)
  112-54; (*core-1,Serial5 --> aggregation-108,Serial1*)
  96-95; (*aggregation-161,Serial1 --> core-33,Serial2*)
  82-124; (*aggregation-171,Serial11 --> core-23,Serial7*)
  80-107; (*aggregation-173,Serial3 --> core-35,Serial5*)
  57-95; (*aggregation-101,Serial1 --> core-33,Serial1*)
  21-37; (*edge-118,Serial4 --> aggregation-112,Serial4*)
  86-106; (*edge-175,Serial2 --> aggregation-170,Serial5*)
  3-27; (*edge-129,Serial2 --> aggregation-122,Serial1*)
  7-160; (*aggregation-134,Serial0 --> core-12,Serial1*)
  91-168; (*aggregation-168,Serial10 --> edge-178,Serial0*)
  29-120; (*aggregation-120,Serial5 --> core-5,Serial9*)
  126-49; (*edge-104,Serial3 --> aggregation-99,Serial2*)
  37-141; (*aggregation-112,Serial10 --> core-28,Serial6*)
  32-128; (*aggregation-61,Serial11 --> core-6,Serial10*)
  51-172; (*aggregation-98,Serial11 --> core-17,Serial0*)
  114-136; (*core-0,Serial4 --> aggregation-156,Serial0*)
  8-125; (*aggregation-133,Serial2 --> core-7,Serial11*)
  89-123; (*aggregation-36,Serial11 --> edge-47,Serial0*)
  151-138; (*edge-59,Serial3 --> aggregation-51,Serial11*)
  167-91; (*edge-179,Serial0 --> aggregation-168,Serial11*)
  49-135; (*aggregation-99,Serial3 --> edge-105,Serial3*)
  125-32; (*core-7,Serial10 --> aggregation-61,Serial9*)
  151-148; (*edge-59,Serial5 --> aggregation-53,Serial9*)
  70-141; (*aggregation-88,Serial1 --> core-28,Serial9*)
  102-153; (*edge-154,Serial3 --> aggregation-147,Serial10*)
  120-54; (*core-5,Serial5 --> aggregation-108,Serial5*)
  99-0; (*core-31,Serial8 --> aggregation-137,Serial11*)
  131-8; (*core-9,Serial11 --> aggregation-133,Serial4*)
  129-47; (*edge-103,Serial0 --> aggregation-96,Serial7*)
  9-114; (*aggregation-132,Serial0 --> core-0,Serial10*)
  15-48; (*aggregation-74,Serial7 --> edge-81,Serial2*)
  80-166; (*aggregation-173,Serial6 --> edge-176,Serial5*)
  9-115; (*aggregation-132,Serial3 --> core-3,Serial10*)
  53-25; (*aggregation-109,Serial8 --> edge-116,Serial1*)
  128-53; (*core-6,Serial4 --> aggregation-109,Serial0*)
  80-167; (*aggregation-173,Serial9 --> edge-179,Serial5*)
  175-106; (*core-14,Serial10 --> aggregation-170,Serial10*)
  52-15; (*edge-83,Serial2 --> aggregation-74,Serial9*)
  80-97; (*aggregation-173,Serial0 --> core-32,Serial5*)
  57-99; (*aggregation-101,Serial11 --> core-31,Serial1*)
  162-113; (*core-10,Serial5 --> aggregation-157,Serial8*)
  124-74; (*core-23,Serial2 --> aggregation-39,Serial11*)
  156-128; (*aggregation-49,Serial0 --> core-6,Serial6*)
  167-106; (*edge-179,Serial2 --> aggregation-170,Serial9*)
  81-142; (*aggregation-172,Serial9 --> core-27,Serial4*)
  127-2; (*core-22,Serial10 --> aggregation-135,Serial10*)
  166-81; (*edge-176,Serial4 --> aggregation-172,Serial2*)
  0-95; (*aggregation-137,Serial1 --> core-33,Serial8*)
  48-17; (*edge-81,Serial4 --> aggregation-76,Serial3*)
  43-154; (*edge-153,Serial2 --> aggregation-146,Serial9*)
  0-59; (*aggregation-137,Serial6 --> edge-140,Serial5*)
  82-168; (*aggregation-171,Serial6 --> edge-178,Serial3*)
  18-13; (*aggregation-125,Serial2 --> edge-130,Serial5*)
  141-58; (*core-28,Serial2 --> aggregation-100,Serial10*)
  54-115; (*aggregation-108,Serial3 --> core-3,Serial5*)
  155-148; (*edge-55,Serial5 --> aggregation-53,Serial5*)
  171-49; (*core-18,Serial0 --> aggregation-99,Serial6*)
  7-174; (*aggregation-134,Serial10 --> core-15,Serial1*)
  33-142; (*aggregation-64,Serial9 --> core-27,Serial0*)
  33-145; (*aggregation-64,Serial6 --> core-24,Serial0*)
  107-150; (*core-35,Serial11 --> aggregation-149,Serial3*)
  24-40; (*edge-117,Serial2 --> aggregation-110,Serial9*)
  109-33; (*edge-71,Serial0 --> aggregation-64,Serial5*)
  146-76; (*edge-42,Serial2 --> aggregation-38,Serial1*)
  28-11; (*aggregation-121,Serial3 --> edge-131,Serial1*)
  74-121; (*aggregation-39,Serial4 --> edge-46,Serial3*)
  108-32; (*edge-70,Serial3 --> aggregation-61,Serial4*)
  125-163; (*core-7,Serial2 --> aggregation-145,Serial1*)
  30-79; (*aggregation-60,Serial5 --> edge-69,Serial2*)
  99-35; (*core-31,Serial0 --> aggregation-65,Serial11*)
  76-160; (*aggregation-38,Serial0 --> core-12,Serial2*)
  134-68; (*core-8,Serial7 --> aggregation-85,Serial2*)
  80-100; (*aggregation-173,Serial10 --> core-30,Serial5*)
  100-18; (*core-30,Serial10 --> aggregation-125,Serial8*)
  100-57; (*core-30,Serial1 --> aggregation-101,Serial10*)
  54-122; (*aggregation-108,Serial4 --> core-4,Serial5*)
  156-131; (*aggregation-49,Serial3 --> core-9,Serial6*)
  105-110; (*edge-163,Serial5 --> aggregation-159,Serial1*)
  167-90; (*edge-179,Serial1 --> aggregation-169,Serial11*)
  14-133; (*aggregation-75,Serial8 --> core-20,Serial3*)
  141-19; (*core-28,Serial11 --> aggregation-124,Serial8*)
  0-39; (*aggregation-137,Serial9 --> edge-143,Serial5*)
  97-148; (*core-32,Serial7 --> aggregation-53,Serial0*)
  140-151; (*aggregation-50,Serial11 --> edge-59,Serial2*)
  152-44; (*aggregation-148,Serial6 --> edge-152,Serial4*)
  139-81; (*core-29,Serial4 --> aggregation-172,Serial11*)
  87-91; (*edge-174,Serial0 --> aggregation-168,Serial6*)
  60-114; (*aggregation-84,Serial0 --> core-0,Serial8*)
  179-119; (*aggregation-41,Serial7 --> edge-45,Serial5*)
  175-31; (*core-14,Serial11 --> aggregation-62,Serial8*)
  36-95; (*aggregation-113,Serial1 --> core-33,Serial6*)
  162-45; (*core-10,Serial0 --> aggregation-97,Serial11*)
  7-55; (*aggregation-134,Serial2 --> edge-139,Serial2*)
  28-131; (*aggregation-121,Serial7 --> core-9,Serial9*)
  5-26; (*edge-128,Serial3 --> aggregation-123,Serial0*)
  110-103; (*aggregation-159,Serial0 --> edge-162,Serial5*)
  35-99; (*aggregation-65,Serial11 --> core-31,Serial0*)
  37-24; (*aggregation-112,Serial3 --> edge-117,Serial4*)
  128-113; (*core-6,Serial5 --> aggregation-157,Serial11*)
  45-118; (*aggregation-97,Serial7 --> edge-107,Serial1*)
  17-142; (*aggregation-76,Serial9 --> core-27,Serial5*)
  145-37; (*core-24,Serial6 --> aggregation-112,Serial6*)
  66-172; (*aggregation-86,Serial5 --> core-17,Serial8*)
  97-16; (*core-32,Serial4 --> aggregation-77,Serial0*)
  88-161; (*aggregation-37,Serial0 --> core-11,Serial1*)
  90-161; (*aggregation-169,Serial5 --> core-11,Serial8*)
  146-89; (*edge-42,Serial0 --> aggregation-36,Serial6*)
  115-177; (*core-3,Serial6 --> aggregation-48,Serial3*)
  70-142; (*aggregation-88,Serial0 --> core-27,Serial9*)
  51-175; (*aggregation-98,Serial8 --> core-14,Serial0*)
  161-10; (*core-11,Serial3 --> aggregation-73,Serial5*)
  56-9; (*edge-138,Serial0 --> aggregation-132,Serial6*)
  63-66; (*edge-90,Serial2 --> aggregation-86,Serial6*)
  165-115; (*aggregation-144,Serial3 --> core-3,Serial1*)
  127-49; (*core-22,Serial0 --> aggregation-99,Serial10*)
  98-104; (*aggregation-160,Serial2 --> edge-164,Serial0*)
  12-52; (*aggregation-72,Serial11 --> edge-83,Serial0*)
  72-64; (*aggregation-87,Serial11 --> edge-95,Serial3*)
  113-85; (*aggregation-157,Serial5 --> edge-167,Serial3*)
  45-134; (*aggregation-97,Serial8 --> core-8,Serial0*)
  85-98; (*edge-167,Serial0 --> aggregation-160,Serial5*)
  135-51; (*edge-105,Serial2 --> aggregation-98,Serial3*)
  85-110; (*edge-167,Serial5 --> aggregation-159,Serial5*)
  52-14; (*edge-83,Serial3 --> aggregation-75,Serial7*)
  151-149; (*edge-59,Serial4 --> aggregation-52,Serial9*)
  115-29; (*core-3,Serial9 --> aggregation-120,Serial9*)
  37-142; (*aggregation-112,Serial9 --> core-27,Serial6*)
  144-58; (*core-25,Serial2 --> aggregation-100,Serial7*)
  172-15; (*core-17,Serial3 --> aggregation-74,Serial1*)
  8-134; (*aggregation-133,Serial3 --> core-8,Serial11*)
  42-152; (*edge-150,Serial4 --> aggregation-148,Serial4*)
  54-23; (*aggregation-108,Serial6 --> edge-114,Serial0*)
  28-161; (*aggregation-121,Serial9 --> core-11,Serial9*)
  19-142; (*aggregation-124,Serial7 --> core-27,Serial11*)
  144-170; (*core-25,Serial3 --> aggregation-40,Serial7*)
  115-165; (*core-3,Serial1 --> aggregation-144,Serial3*)
  174-27; (*core-15,Serial9 --> aggregation-122,Serial7*)
  111-176; (*aggregation-158,Serial7 --> core-13,Serial7*)
  33-77; (*aggregation-64,Serial1 --> edge-67,Serial0*)
  129-45; (*edge-103,Serial1 --> aggregation-97,Serial3*)
  172-31; (*core-17,Serial11 --> aggregation-62,Serial11*)
  96-107; (*aggregation-161,Serial9 --> core-35,Serial2*)
  159-156; (*edge-58,Serial1 --> aggregation-49,Serial10*)
  58-129; (*aggregation-100,Serial1 --> edge-103,Serial4*)
  61-9; (*edge-142,Serial0 --> aggregation-132,Serial10*)
  66-71; (*aggregation-86,Serial9 --> edge-93,Serial2*)
  138-124; (*aggregation-51,Serial1 --> core-23,Serial6*)
  3-28; (*edge-129,Serial1 --> aggregation-121,Serial1*)
  99-96; (*core-31,Serial2 --> aggregation-161,Serial11*)
  26-130; (*aggregation-123,Serial7 --> core-21,Serial9*)
  140-172; (*aggregation-50,Serial5 --> core-17,Serial6*)
  67-70; (*edge-91,Serial4 --> aggregation-88,Serial7*)
  2-39; (*aggregation-135,Serial5 --> edge-143,Serial3*)
  58-145; (*aggregation-100,Serial6 --> core-24,Serial2*)
  40-175; (*aggregation-110,Serial2 --> core-14,Serial5*)
  163-128; (*aggregation-145,Serial0 --> core-6,Serial2*)
  100-16; (*core-30,Serial4 --> aggregation-77,Serial10*)
  120-12; (*core-5,Serial3 --> aggregation-72,Serial5*)
  64-72; (*edge-95,Serial3 --> aggregation-87,Serial11*)
  118-45; (*edge-107,Serial1 --> aggregation-97,Serial7*)
  35-109; (*aggregation-65,Serial7 --> edge-71,Serial1*)
  162-88; (*core-10,Serial1 --> aggregation-37,Serial11*)
  59-2; (*edge-140,Serial3 --> aggregation-135,Serial2*)
  149-158; (*aggregation-52,Serial7 --> edge-57,Serial4*)
  124-38; (*core-23,Serial5 --> aggregation-111,Serial2*)
  136-85; (*aggregation-156,Serial7 --> edge-167,Serial2*)
  25-54; (*edge-116,Serial0 --> aggregation-108,Serial8*)
  163-44; (*aggregation-145,Serial8 --> edge-152,Serial1*)
  178-140; (*edge-54,Serial2 --> aggregation-50,Serial6*)
  2-127; (*aggregation-135,Serial10 --> core-22,Serial10*)
  53-125; (*aggregation-109,Serial1 --> core-7,Serial4*)
  179-100; (*aggregation-41,Serial10 --> core-30,Serial3*)
  90-131; (*aggregation-169,Serial3 --> core-9,Serial8*)
  7-61; (*aggregation-134,Serial5 --> edge-142,Serial2*)
  30-120; (*aggregation-60,Serial11 --> core-5,Serial11*)
  71-60; (*edge-93,Serial0 --> aggregation-84,Serial9*)
  19-3; (*aggregation-124,Serial1 --> edge-129,Serial4*)
  15-174; (*aggregation-74,Serial11 --> core-15,Serial3*)
  124-72; (*core-23,Serial8 --> aggregation-87,Serial5*)
  12-120; (*aggregation-72,Serial5 --> core-5,Serial3*)
  29-6; (*aggregation-120,Serial11 --> edge-127,Serial0*)
  154-102; (*aggregation-146,Serial10 --> edge-154,Serial2*)
  45-129; (*aggregation-97,Serial3 --> edge-103,Serial1*)
  93-12; (*edge-79,Serial0 --> aggregation-72,Serial7*)
  55-2; (*edge-139,Serial3 --> aggregation-135,Serial1*)
  112-12; (*core-1,Serial3 --> aggregation-72,Serial1*)
  34-78; (*aggregation-63,Serial2 --> edge-68,Serial5*)
  58-141; (*aggregation-100,Serial10 --> core-28,Serial2*)
  72-67; (*aggregation-87,Serial7 --> edge-91,Serial3*)
  95-80; (*core-33,Serial5 --> aggregation-173,Serial1*)
  124-14; (*core-23,Serial3 --> aggregation-75,Serial11*)
  29-4; (*aggregation-120,Serial10 --> edge-126,Serial0*)
  74-146; (*aggregation-39,Serial0 --> edge-42,Serial3*)
  127-82; (*core-22,Serial7 --> aggregation-171,Serial10*)
  83-113; (*edge-166,Serial3 --> aggregation-157,Serial4*)
  150-102; (*aggregation-149,Serial8 --> edge-154,Serial5*)
  171-2; (*core-18,Serial10 --> aggregation-135,Serial6*)
  17-52; (*aggregation-76,Serial5 --> edge-83,Serial4*)
  92-12; (*edge-78,Serial0 --> aggregation-72,Serial6*)
  17-46; (*aggregation-76,Serial2 --> edge-80,Serial4*)
  39-7; (*edge-143,Serial2 --> aggregation-134,Serial6*)
  63-72; (*edge-90,Serial3 --> aggregation-87,Serial6*)
  94-96; (*core-34,Serial2 --> aggregation-161,Serial8*)
  9-117; (*aggregation-132,Serial2 --> core-2,Serial10*)
  100-65; (*core-30,Serial9 --> aggregation-89,Serial5*)
  177-159; (*aggregation-48,Serial10 --> edge-58,Serial0*)
  70-63; (*aggregation-88,Serial6 --> edge-90,Serial4*)
  68-162; (*aggregation-85,Serial4 --> core-10,Serial7*)
  36-99; (*aggregation-113,Serial11 --> core-31,Serial6*)
  165-112; (*aggregation-144,Serial1 --> core-1,Serial1*)
  112-30; (*core-1,Serial11 --> aggregation-60,Serial1*)
  98-103; (*aggregation-160,Serial0 --> edge-162,Serial0*)
  75-35; (*edge-66,Serial1 --> aggregation-65,Serial2*)
  12-112; (*aggregation-72,Serial1 --> core-1,Serial3*)
  140-160; (*aggregation-50,Serial0 --> core-12,Serial6*)
  49-129; (*aggregation-99,Serial1 --> edge-103,Serial3*)
  162-163; (*core-10,Serial2 --> aggregation-145,Serial4*)
  165-42; (*aggregation-144,Serial6 --> edge-150,Serial0*)
  11-26; (*edge-131,Serial3 --> aggregation-123,Serial3*)
  112-60; (*core-1,Serial8 --> aggregation-84,Serial1*)
  145-152; (*core-24,Serial10 --> aggregation-148,Serial2*)
  122-47; (*core-4,Serial0 --> aggregation-96,Serial4*)
  37-25; (*aggregation-112,Serial2 --> edge-116,Serial4*)
  125-45; (*core-7,Serial0 --> aggregation-97,Serial10*)
  172-27; (*core-17,Serial9 --> aggregation-122,Serial9*)
  111-160; (*aggregation-158,Serial6 --> core-12,Serial7*)
  125-28; (*core-7,Serial9 --> aggregation-121,Serial5*)
  160-40; (*core-12,Serial5 --> aggregation-110,Serial0*)
  154-174; (*aggregation-146,Serial3 --> core-15,Serial4*)
  176-154; (*core-13,Serial4 --> aggregation-146,Serial1*)
  16-100; (*aggregation-77,Serial10 --> core-30,Serial4*)
  59-8; (*edge-140,Serial1 --> aggregation-133,Serial7*)
  53-22; (*aggregation-109,Serial7 --> edge-115,Serial1*)
  64-68; (*edge-95,Serial1 --> aggregation-85,Serial11*)
  124-138; (*core-23,Serial6 --> aggregation-51,Serial1*)
  36-25; (*aggregation-113,Serial6 --> edge-116,Serial5*)
  116-74; (*edge-44,Serial3 --> aggregation-39,Serial2*)
  100-80; (*core-30,Serial5 --> aggregation-173,Serial10*)
  44-165; (*edge-152,Serial0 --> aggregation-144,Serial8*)
  106-168; (*aggregation-170,Serial8 --> edge-178,Serial2*)
  45-162; (*aggregation-97,Serial11 --> core-10,Serial0*)
  171-72; (*core-18,Serial8 --> aggregation-87,Serial0*)
  78-32; (*edge-68,Serial3 --> aggregation-61,Serial2*)
  170-119; (*aggregation-40,Serial3 --> edge-45,Serial4*)
  114-47; (*core-0,Serial0 --> aggregation-96,Serial0*)
  16-107; (*aggregation-77,Serial3 --> core-35,Serial4*)
  155-149; (*edge-55,Serial4 --> aggregation-52,Serial5*)
  156-155; (*aggregation-49,Serial7 --> edge-55,Serial1*)
  10-128; (*aggregation-73,Serial0 --> core-6,Serial3*)
  110-169; (*aggregation-159,Serial7 --> core-19,Serial4*)
  33-141; (*aggregation-64,Serial10 --> core-28,Serial0*)
  44-153; (*edge-152,Serial3 --> aggregation-147,Serial8*)
  176-51; (*core-13,Serial0 --> aggregation-98,Serial7*)
  16-52; (*aggregation-77,Serial9 --> edge-83,Serial5*)
  76-123; (*aggregation-38,Serial6 --> edge-47,Serial2*)
  18-97; (*aggregation-125,Serial5 --> core-32,Serial10*)
  95-65; (*core-33,Serial9 --> aggregation-89,Serial1*)
  66-160; (*aggregation-86,Serial0 --> core-12,Serial8*)
  70-145; (*aggregation-88,Serial4 --> core-24,Serial9*)
  169-153; (*core-19,Serial1 --> aggregation-147,Serial3*)
  32-109; (*aggregation-61,Serial5 --> edge-71,Serial3*)
  111-172; (*aggregation-158,Serial11 --> core-17,Serial7*)
  123-76; (*edge-47,Serial2 --> aggregation-38,Serial6*)
  143-33; (*core-26,Serial0 --> aggregation-64,Serial8*)
  18-107; (*aggregation-125,Serial7 --> core-35,Serial10*)
  146-88; (*edge-42,Serial1 --> aggregation-37,Serial5*)
  62-9; (*edge-141,Serial0 --> aggregation-132,Serial9*)
  133-34; (*core-20,Serial11 --> aggregation-63,Serial8*)
  18-99; (*aggregation-125,Serial9 --> core-31,Serial10*)
  103-113; (*edge-162,Serial3 --> aggregation-157,Serial0*)
  110-83; (*aggregation-159,Serial4 --> edge-166,Serial5*)
  110-105; (*aggregation-159,Serial1 --> edge-163,Serial5*)
  30-77; (*aggregation-60,Serial3 --> edge-67,Serial2*)
  141-152; (*core-28,Serial10 --> aggregation-148,Serial10*)
  170-142; (*aggregation-40,Serial9 --> core-27,Serial3*)
  51-176; (*aggregation-98,Serial7 --> core-13,Serial0*)
  57-100; (*aggregation-101,Serial10 --> core-30,Serial1*)
  155-156; (*edge-55,Serial1 --> aggregation-49,Serial7*)
  95-96; (*core-33,Serial2 --> aggregation-161,Serial1*)
  0-99; (*aggregation-137,Serial11 --> core-31,Serial8*)
  4-28; (*edge-126,Serial1 --> aggregation-121,Serial10*)
  49-130; (*aggregation-99,Serial9 --> core-21,Serial0*)
  177-157; (*aggregation-48,Serial8 --> edge-56,Serial0*)
  121-88; (*edge-46,Serial1 --> aggregation-37,Serial9*)
  28-13; (*aggregation-121,Serial2 --> edge-130,Serial1*)
  108-34; (*edge-70,Serial5 --> aggregation-63,Serial4*)
  115-89; (*core-3,Serial2 --> aggregation-36,Serial3*)
  33-143; (*aggregation-64,Serial8 --> core-26,Serial0*)
  104-96; (*edge-164,Serial1 --> aggregation-161,Serial4*)
  6-29; (*edge-127,Serial0 --> aggregation-120,Serial11*)
  43-163; (*edge-153,Serial1 --> aggregation-145,Serial9*)
  56-1; (*edge-138,Serial4 --> aggregation-136,Serial0*)
  77-34; (*edge-67,Serial5 --> aggregation-63,Serial1*)
  118-49; (*edge-107,Serial3 --> aggregation-99,Serial5*)
  130-49; (*core-21,Serial0 --> aggregation-99,Serial9*)
  5-29; (*edge-128,Serial0 --> aggregation-120,Serial0*)
  28-128; (*aggregation-121,Serial4 --> core-6,Serial9*)
  73-66; (*edge-94,Serial2 --> aggregation-86,Serial10*)
  101-163; (*edge-155,Serial1 --> aggregation-145,Serial11*)
  159-140; (*edge-58,Serial2 --> aggregation-50,Serial10*)
  173-40; (*core-16,Serial5 --> aggregation-110,Serial4*)
  31-79; (*aggregation-62,Serial3 --> edge-69,Serial4*)
  51-126; (*aggregation-98,Serial2 --> edge-104,Serial2*)
  57-107; (*aggregation-101,Serial3 --> core-35,Serial1*)
  84-111; (*edge-165,Serial4 --> aggregation-158,Serial3*)
  138-159; (*aggregation-51,Serial10 --> edge-58,Serial3*)
  68-125; (*aggregation-85,Serial1 --> core-7,Serial7*)
  124-2; (*core-23,Serial10 --> aggregation-135,Serial11*)
  19-4; (*aggregation-124,Serial10 --> edge-126,Serial4*)
  15-50; (*aggregation-74,Serial8 --> edge-82,Serial2*)
  54-114; (*aggregation-108,Serial0 --> core-0,Serial5*)
  153-133; (*aggregation-147,Serial4 --> core-20,Serial1*)
  71-65; (*edge-93,Serial5 --> aggregation-89,Serial9*)
  128-90; (*core-6,Serial8 --> aggregation-169,Serial0*)
  133-74; (*core-20,Serial2 --> aggregation-39,Serial8*)
  42-150; (*edge-150,Serial5 --> aggregation-149,Serial4*)
  2-56; (*aggregation-135,Serial0 --> edge-138,Serial3*)
  19-141; (*aggregation-124,Serial8 --> core-28,Serial11*)
  98-143; (*aggregation-160,Serial8 --> core-26,Serial1*)
  89-117; (*aggregation-36,Serial2 --> core-2,Serial2*)
  82-166; (*aggregation-171,Serial4 --> edge-176,Serial3*)
  110-133; (*aggregation-159,Serial8 --> core-20,Serial4*)
  60-73; (*aggregation-84,Serial10 --> edge-94,Serial0*)
  72-124; (*aggregation-87,Serial5 --> core-23,Serial8*)
  172-51; (*core-17,Serial0 --> aggregation-98,Serial11*)
  45-125; (*aggregation-97,Serial10 --> core-7,Serial0*)
  13-19; (*edge-130,Serial4 --> aggregation-124,Serial2*)
  138-178; (*aggregation-51,Serial6 --> edge-54,Serial3*)
  99-36; (*core-31,Serial6 --> aggregation-113,Serial11*)
  58-132; (*aggregation-100,Serial4 --> edge-106,Serial4*)
  173-7; (*core-16,Serial1 --> aggregation-134,Serial7*)
  133-26; (*core-20,Serial9 --> aggregation-123,Serial6*)
  86-90; (*edge-175,Serial1 --> aggregation-169,Serial7*)
  53-134; (*aggregation-109,Serial2 --> core-8,Serial4*)
  7-39; (*aggregation-134,Serial6 --> edge-143,Serial2*)
  25-37; (*edge-116,Serial4 --> aggregation-112,Serial2*)
  145-81; (*core-24,Serial4 --> aggregation-172,Serial6*)
  133-110; (*core-20,Serial4 --> aggregation-159,Serial8*)
  10-50; (*aggregation-73,Serial10 --> edge-82,Serial1*)
  67-68; (*edge-91,Serial1 --> aggregation-85,Serial7*)
  79-30; (*edge-69,Serial2 --> aggregation-60,Serial5*)
  116-170; (*edge-44,Serial4 --> aggregation-40,Serial2*)
  148-157; (*aggregation-53,Serial6 --> edge-56,Serial5*)
  125-88; (*core-7,Serial1 --> aggregation-37,Serial2*)
  96-99; (*aggregation-161,Serial11 --> core-31,Serial2*)
  165-101; (*aggregation-144,Serial11 --> edge-155,Serial0*)
  118-57; (*edge-107,Serial5 --> aggregation-101,Serial9*)
  124-34; (*core-23,Serial11 --> aggregation-63,Serial11*)
  160-106; (*core-12,Serial10 --> aggregation-170,Serial2*)
  163-41; (*aggregation-145,Serial7 --> edge-151,Serial1*)
  100-150; (*core-30,Serial11 --> aggregation-149,Serial10*)
  172-154; (*core-17,Serial4 --> aggregation-146,Serial5*)
  84-136; (*edge-165,Serial2 --> aggregation-156,Serial5*)
  23-36; (*edge-114,Serial5 --> aggregation-113,Serial4*)
  60-120; (*aggregation-84,Serial5 --> core-5,Serial8*)
  95-179; (*core-33,Serial3 --> aggregation-41,Serial1*)
  168-82; (*edge-178,Serial3 --> aggregation-171,Serial6*)
  102-150; (*edge-154,Serial5 --> aggregation-149,Serial8*)
  36-20; (*aggregation-113,Serial9 --> edge-119,Serial5*)
  151-177; (*edge-59,Serial0 --> aggregation-48,Serial11*)
  98-141; (*aggregation-160,Serial10 --> core-28,Serial1*)
  110-127; (*aggregation-159,Serial10 --> core-22,Serial4*)
  161-68; (*core-11,Serial7 --> aggregation-85,Serial5*)
  142-70; (*core-27,Serial9 --> aggregation-88,Serial0*)
  11-29; (*edge-131,Serial0 --> aggregation-120,Serial3*)
  65-73; (*aggregation-89,Serial10 --> edge-94,Serial5*)
  125-10; (*core-7,Serial3 --> aggregation-73,Serial1*)
  62-1; (*edge-141,Serial4 --> aggregation-136,Serial3*)
  26-133; (*aggregation-123,Serial6 --> core-20,Serial9*)
  175-140; (*core-14,Serial6 --> aggregation-50,Serial2*)
  24-53; (*edge-117,Serial1 --> aggregation-109,Serial9*)
  179-121; (*aggregation-41,Serial8 --> edge-46,Serial5*)
  144-149; (*core-25,Serial8 --> aggregation-52,Serial3*)
  161-53; (*core-11,Serial4 --> aggregation-109,Serial5*)
  25-38; (*edge-116,Serial3 --> aggregation-111,Serial6*)
  27-174; (*aggregation-122,Serial7 --> core-15,Serial9*)
  60-63; (*aggregation-84,Serial6 --> edge-90,Serial0*)
  46-10; (*edge-80,Serial1 --> aggregation-73,Serial8*)
  40-176; (*aggregation-110,Serial1 --> core-13,Serial5*)
  21-36; (*edge-118,Serial5 --> aggregation-113,Serial8*)
  118-47; (*edge-107,Serial0 --> aggregation-96,Serial11*)
  72-69; (*aggregation-87,Serial8 --> edge-92,Serial3*)
  129-57; (*edge-103,Serial5 --> aggregation-101,Serial5*)
  163-131; (*aggregation-145,Serial3 --> core-9,Serial2*)
  97-36; (*core-32,Serial6 --> aggregation-113,Serial0*)
  27-5; (*aggregation-122,Serial0 --> edge-128,Serial2*)
  15-175; (*aggregation-74,Serial10 --> core-14,Serial3*)
  150-94; (*aggregation-149,Serial2 --> core-34,Serial11*)
  39-0; (*edge-143,Serial5 --> aggregation-137,Serial9*)
  31-77; (*aggregation-62,Serial1 --> edge-67,Serial4*)
  178-138; (*edge-54,Serial3 --> aggregation-51,Serial6*)
  12-122; (*aggregation-72,Serial4 --> core-4,Serial3*)
  141-170; (*core-28,Serial3 --> aggregation-40,Serial10*)
  165-43; (*aggregation-144,Serial9 --> edge-153,Serial0*)
  89-119; (*aggregation-36,Serial9 --> edge-45,Serial0*)
  117-91; (*core-2,Serial7 --> aggregation-168,Serial2*)
  29-11; (*aggregation-120,Serial3 --> edge-131,Serial0*)
  111-173; (*aggregation-158,Serial10 --> core-16,Serial7*)
  142-152; (*core-27,Serial10 --> aggregation-148,Serial0*)
  117-54; (*core-2,Serial5 --> aggregation-108,Serial2*)
  27-172; (*aggregation-122,Serial9 --> core-17,Serial9*)
  46-15; (*edge-80,Serial2 --> aggregation-74,Serial6*)
  70-144; (*aggregation-88,Serial5 --> core-25,Serial9*)
  166-80; (*edge-176,Serial5 --> aggregation-173,Serial6*)
  169-38; (*core-19,Serial5 --> aggregation-111,Serial0*)
  156-178; (*aggregation-49,Serial6 --> edge-54,Serial1*)
  26-6; (*aggregation-123,Serial11 --> edge-127,Serial3*)
  139-152; (*core-29,Serial10 --> aggregation-148,Serial1*)
  44-154; (*edge-152,Serial2 --> aggregation-146,Serial8*)
  117-136; (*core-2,Serial4 --> aggregation-156,Serial8*)
  143-17; (*core-26,Serial5 --> aggregation-76,Serial8*)
  53-131; (*aggregation-109,Serial3 --> core-9,Serial4*)
  60-69; (*aggregation-84,Serial8 --> edge-92,Serial0*)
  113-131; (*aggregation-157,Serial7 --> core-9,Serial5*)
  147-179; (*edge-43,Serial5 --> aggregation-41,Serial5*)
  78-31; (*edge-68,Serial4 --> aggregation-62,Serial2*)
  107-35; (*core-35,Serial0 --> aggregation-65,Serial9*)
  14-92; (*aggregation-75,Serial2 --> edge-78,Serial3*)
  87-106; (*edge-174,Serial2 --> aggregation-170,Serial4*)
  169-138; (*core-19,Serial6 --> aggregation-51,Serial3*)
  126-47; (*edge-104,Serial0 --> aggregation-96,Serial8*)
  95-57; (*core-33,Serial1 --> aggregation-101,Serial1*)
  1-143; (*aggregation-136,Serial8 --> core-26,Serial7*)
  38-169; (*aggregation-111,Serial0 --> core-19,Serial5*)
  97-150; (*core-32,Serial11 --> aggregation-149,Serial0*)
  129-49; (*edge-103,Serial3 --> aggregation-99,Serial1*)
  134-88; (*core-8,Serial1 --> aggregation-37,Serial3*)
  161-28; (*core-11,Serial9 --> aggregation-121,Serial9*)
  28-125; (*aggregation-121,Serial5 --> core-7,Serial9*)
  173-66; (*core-16,Serial8 --> aggregation-86,Serial4*)
  130-26; (*core-21,Serial9 --> aggregation-123,Serial7*)
  159-138; (*edge-58,Serial3 --> aggregation-51,Serial10*)
  169-82; (*core-19,Serial7 --> aggregation-171,Serial0*)
  89-114; (*aggregation-36,Serial0 --> core-0,Serial2*)
  5-27; (*edge-128,Serial2 --> aggregation-122,Serial0*)
  82-130; (*aggregation-171,Serial9 --> core-21,Serial7*)
  97-35; (*core-32,Serial0 --> aggregation-65,Serial0*)
  36-24; (*aggregation-113,Serial7 --> edge-117,Serial5*)
  128-156; (*core-6,Serial6 --> aggregation-49,Serial0*)
  33-75; (*aggregation-64,Serial0 --> edge-66,Serial0*)
  169-14; (*core-19,Serial3 --> aggregation-75,Serial0*)
  117-9; (*core-2,Serial10 --> aggregation-132,Serial2*)
  77-33; (*edge-67,Serial0 --> aggregation-64,Serial1*)
  97-0; (*core-32,Serial8 --> aggregation-137,Serial0*)
  126-58; (*edge-104,Serial4 --> aggregation-100,Serial2*)
  87-82; (*edge-174,Serial3 --> aggregation-171,Serial2*)
  122-30; (*core-4,Serial11 --> aggregation-60,Serial10*)
  51-135; (*aggregation-98,Serial3 --> edge-105,Serial2*)
  82-86; (*aggregation-171,Serial3 --> edge-175,Serial3*)
  20-37; (*edge-119,Serial4 --> aggregation-112,Serial5*)
  143-98; (*core-26,Serial1 --> aggregation-160,Serial8*)
  46-17; (*edge-80,Serial4 --> aggregation-76,Serial2*)
  39-8; (*edge-143,Serial1 --> aggregation-133,Serial10*)
  49-137; (*aggregation-99,Serial0 --> edge-102,Serial3*)
  175-76; (*core-14,Serial2 --> aggregation-38,Serial9*)
  152-42; (*aggregation-148,Serial4 --> edge-150,Serial4*)
  88-121; (*aggregation-37,Serial9 --> edge-46,Serial1*)
  173-15; (*core-16,Serial3 --> aggregation-74,Serial0*)
  140-158; (*aggregation-50,Serial9 --> edge-57,Serial2*)
  160-66; (*core-12,Serial8 --> aggregation-86,Serial0*)
  4-27; (*edge-126,Serial2 --> aggregation-122,Serial10*)
  111-85; (*aggregation-158,Serial5 --> edge-167,Serial4*)
  169-49; (*core-19,Serial0 --> aggregation-99,Serial7*)
  142-37; (*core-27,Serial6 --> aggregation-112,Serial9*)
  68-128; (*aggregation-85,Serial0 --> core-6,Serial7*)
  144-19; (*core-25,Serial11 --> aggregation-124,Serial5*)
  38-124; (*aggregation-111,Serial2 --> core-23,Serial5*)
  54-24; (*aggregation-108,Serial9 --> edge-117,Serial0*)
  108-33; (*edge-70,Serial0 --> aggregation-64,Serial4*)
  101-153; (*edge-155,Serial3 --> aggregation-147,Serial11*)
  54-112; (*aggregation-108,Serial1 --> core-1,Serial5*)
  107-148; (*core-35,Serial7 --> aggregation-53,Serial3*)
  134-163; (*core-8,Serial2 --> aggregation-145,Serial2*)
  138-157; (*aggregation-51,Serial8 --> edge-56,Serial3*)
  162-10; (*core-10,Serial3 --> aggregation-73,Serial4*)
  91-114; (*aggregation-168,Serial0 --> core-0,Serial7*)
  19-13; (*aggregation-124,Serial2 --> edge-130,Serial4*)
  109-32; (*edge-71,Serial3 --> aggregation-61,Serial5*)
  159-148; (*edge-58,Serial5 --> aggregation-53,Serial8*)
  177-151; (*aggregation-48,Serial11 --> edge-59,Serial0*)
  54-25; (*aggregation-108,Serial8 --> edge-116,Serial0*)
  72-130; (*aggregation-87,Serial3 --> core-21,Serial8*)
  168-106; (*edge-178,Serial2 --> aggregation-170,Serial8*)
  68-161; (*aggregation-85,Serial5 --> core-11,Serial7*)
  165-114; (*aggregation-144,Serial0 --> core-0,Serial1*)
  164-81; (*edge-177,Serial4 --> aggregation-172,Serial3*)
  18-94; (*aggregation-125,Serial6 --> core-34,Serial10*)
  150-101; (*aggregation-149,Serial9 --> edge-155,Serial5*)
  175-15; (*core-14,Serial3 --> aggregation-74,Serial10*)
  33-139; (*aggregation-64,Serial11 --> core-29,Serial0*)
  80-87; (*aggregation-173,Serial4 --> edge-174,Serial5*)
  138-151; (*aggregation-51,Serial11 --> edge-59,Serial3*)
  124-153; (*core-23,Serial1 --> aggregation-147,Serial1*)
  85-96; (*edge-167,Serial1 --> aggregation-161,Serial7*)
  145-1; (*core-24,Serial7 --> aggregation-136,Serial6*)
  19-11; (*aggregation-124,Serial3 --> edge-131,Serial4*)
  174-106; (*core-15,Serial10 --> aggregation-170,Serial11*)
  53-20; (*aggregation-109,Serial11 --> edge-119,Serial1*)
  99-18; (*core-31,Serial10 --> aggregation-125,Serial9*)
  104-110; (*edge-164,Serial5 --> aggregation-159,Serial2*)
  26-169; (*aggregation-123,Serial5 --> core-19,Serial9*)
  109-31; (*edge-71,Serial4 --> aggregation-62,Serial5*)
  160-111; (*core-12,Serial7 --> aggregation-158,Serial6*)
  100-35; (*core-30,Serial0 --> aggregation-65,Serial10*)
  168-90; (*edge-178,Serial1 --> aggregation-169,Serial10*)
  100-0; (*core-30,Serial8 --> aggregation-137,Serial10*)
  52-16; (*edge-83,Serial5 --> aggregation-77,Serial9*)
  25-40; (*edge-116,Serial2 --> aggregation-110,Serial8*)
  51-160; (*aggregation-98,Serial6 --> core-12,Serial0*)
  110-104; (*aggregation-159,Serial2 --> edge-164,Serial5*)
  131-68; (*core-9,Serial7 --> aggregation-85,Serial3*)
  168-91; (*edge-178,Serial0 --> aggregation-168,Serial10*)
  145-70; (*core-24,Serial9 --> aggregation-88,Serial4*)
  139-58; (*core-29,Serial2 --> aggregation-100,Serial11*)
  174-31; (*core-15,Serial11 --> aggregation-62,Serial9*)
  178-148; (*edge-54,Serial5 --> aggregation-53,Serial4*)
  99-57; (*core-31,Serial1 --> aggregation-101,Serial11*)
  31-160; (*aggregation-62,Serial6 --> core-12,Serial11*)
  53-21; (*aggregation-109,Serial10 --> edge-118,Serial1*)
  147-76; (*edge-43,Serial2 --> aggregation-38,Serial2*)
  95-16; (*core-33,Serial4 --> aggregation-77,Serial1*)
  117-29; (*core-2,Serial9 --> aggregation-120,Serial8*)
  159-149; (*edge-58,Serial4 --> aggregation-52,Serial8*)
  36-21; (*aggregation-113,Serial8 --> edge-118,Serial5*)
  80-94; (*aggregation-173,Serial2 --> core-34,Serial5*)
  110-84; (*aggregation-159,Serial3 --> edge-165,Serial5*)
  18-3; (*aggregation-125,Serial1 --> edge-129,Serial5*)
  163-134; (*aggregation-145,Serial2 --> core-8,Serial2*)
  122-177; (*core-4,Serial6 --> aggregation-48,Serial4*)
  172-106; (*core-17,Serial10 --> aggregation-170,Serial1*)
  139-19; (*core-29,Serial11 --> aggregation-124,Serial9*)
  113-134; (*aggregation-157,Serial6 --> core-8,Serial5*)
  155-140; (*edge-55,Serial2 --> aggregation-50,Serial7*)
  160-27; (*core-12,Serial9 --> aggregation-122,Serial4*)
  30-75; (*aggregation-60,Serial2 --> edge-66,Serial2*)
  11-28; (*edge-131,Serial1 --> aggregation-121,Serial3*)
  55-9; (*edge-139,Serial0 --> aggregation-132,Serial7*)
  58-135; (*aggregation-100,Serial3 --> edge-105,Serial4*)
  1-139; (*aggregation-136,Serial11 --> core-29,Serial7*)
  1-39; (*aggregation-136,Serial5 --> edge-143,Serial4*)
  14-127; (*aggregation-75,Serial10 --> core-22,Serial3*)
  9-112; (*aggregation-132,Serial1 --> core-1,Serial10*)
  67-66; (*edge-91,Serial2 --> aggregation-86,Serial7*)
  149-141; (*aggregation-52,Serial10 --> core-28,Serial8*)
  99-16; (*core-31,Serial4 --> aggregation-77,Serial11*)
  10-52; (*aggregation-73,Serial11 --> edge-83,Serial1*)
  157-177; (*edge-56,Serial0 --> aggregation-48,Serial8*)
  161-45; (*core-11,Serial0 --> aggregation-97,Serial0*)
  65-99; (*aggregation-89,Serial4 --> core-31,Serial9*)
  136-115; (*aggregation-156,Serial9 --> core-3,Serial4*)
  31-173; (*aggregation-62,Serial10 --> core-16,Serial11*)
  86-91; (*edge-175,Serial0 --> aggregation-168,Serial7*)
  151-156; (*edge-59,Serial1 --> aggregation-49,Serial11*)
  153-169; (*aggregation-147,Serial3 --> core-19,Serial1*)
  145-170; (*core-24,Serial3 --> aggregation-40,Serial6*)
  32-161; (*aggregation-61,Serial10 --> core-11,Serial10*)
  82-133; (*aggregation-171,Serial8 --> core-20,Serial7*)
  114-165; (*core-0,Serial1 --> aggregation-144,Serial0*)
  170-116; (*aggregation-40,Serial2 --> edge-44,Serial4*)
  148-99; (*aggregation-53,Serial11 --> core-31,Serial7*)
  1-144; (*aggregation-136,Serial7 --> core-25,Serial7*)
  30-114; (*aggregation-60,Serial0 --> core-0,Serial11*)
  126-51; (*edge-104,Serial2 --> aggregation-98,Serial2*)
  63-70; (*edge-90,Serial4 --> aggregation-88,Serial6*)
  38-133; (*aggregation-111,Serial10 --> core-20,Serial5*)
  126-45; (*edge-104,Serial1 --> aggregation-97,Serial4*)
  134-32; (*core-8,Serial10 --> aggregation-61,Serial6*)
  143-58; (*core-26,Serial2 --> aggregation-100,Serial8*)
  98-144; (*aggregation-160,Serial7 --> core-25,Serial1*)
  53-23; (*aggregation-109,Serial6 --> edge-114,Serial1*)
  60-64; (*aggregation-84,Serial11 --> edge-95,Serial0*)
  119-89; (*edge-45,Serial0 --> aggregation-36,Serial9*)
  38-23; (*aggregation-111,Serial4 --> edge-114,Serial3*)
  0-56; (*aggregation-137,Serial4 --> edge-138,Serial5*)
  165-44; (*aggregation-144,Serial8 --> edge-152,Serial0*)
  60-67; (*aggregation-84,Serial7 --> edge-91,Serial0*)
  154-172; (*aggregation-146,Serial5 --> core-17,Serial4*)
  12-115; (*aggregation-72,Serial3 --> core-3,Serial3*)
  106-173; (*aggregation-170,Serial0 --> core-16,Serial10*)
  13-26; (*edge-130,Serial3 --> aggregation-123,Serial2*)
  122-165; (*core-4,Serial1 --> aggregation-144,Serial4*)
  24-54; (*edge-117,Serial0 --> aggregation-108,Serial9*)
  41-152; (*edge-151,Serial4 --> aggregation-148,Serial5*)
  47-114; (*aggregation-96,Serial0 --> core-0,Serial0*)
  96-103; (*aggregation-161,Serial2 --> edge-162,Serial1*)
  7-176; (*aggregation-134,Serial11 --> core-13,Serial1*)
  27-173; (*aggregation-122,Serial8 --> core-16,Serial9*)
  70-64; (*aggregation-88,Serial11 --> edge-95,Serial4*)
  98-139; (*aggregation-160,Serial11 --> core-29,Serial1*)
  73-72; (*edge-94,Serial3 --> aggregation-87,Serial10*)
  14-93; (*aggregation-75,Serial3 --> edge-79,Serial3*)
  149-151; (*aggregation-52,Serial9 --> edge-59,Serial4*)
  62-2; (*edge-141,Serial3 --> aggregation-135,Serial3*)
  48-12; (*edge-81,Serial0 --> aggregation-72,Serial9*)
  37-22; (*aggregation-112,Serial1 --> edge-115,Serial4*)
  90-125; (*aggregation-169,Serial1 --> core-7,Serial8*)
  133-2; (*core-20,Serial10 --> aggregation-135,Serial8*)
  127-38; (*core-22,Serial5 --> aggregation-111,Serial1*)
  76-176; (*aggregation-38,Serial11 --> core-13,Serial2*)
  34-75; (*aggregation-63,Serial0 --> edge-66,Serial5*)
  17-50; (*aggregation-76,Serial4 --> edge-82,Serial4*)
  31-78; (*aggregation-62,Serial2 --> edge-68,Serial4*)
  143-70; (*core-26,Serial9 --> aggregation-88,Serial3*)
  12-48; (*aggregation-72,Serial9 --> edge-81,Serial0*)
  70-67; (*aggregation-88,Serial7 --> edge-91,Serial4*)
  177-114; (*aggregation-48,Serial0 --> core-0,Serial6*)
  35-94; (*aggregation-65,Serial8 --> core-34,Serial0*)
  46-14; (*edge-80,Serial3 --> aggregation-75,Serial4*)
  150-95; (*aggregation-149,Serial1 --> core-33,Serial11*)
  104-98; (*edge-164,Serial0 --> aggregation-160,Serial2*)
  141-81; (*core-28,Serial4 --> aggregation-172,Serial10*)
  161-88; (*core-11,Serial1 --> aggregation-37,Serial0*)
  28-3; (*aggregation-121,Serial1 --> edge-129,Serial1*)
  72-71; (*aggregation-87,Serial9 --> edge-93,Serial3*)
  142-149; (*core-27,Serial8 --> aggregation-52,Serial0*)
  56-2; (*edge-138,Serial3 --> aggregation-135,Serial0*)
  81-167; (*aggregation-172,Serial5 --> edge-179,Serial4*)
  101-152; (*edge-155,Serial4 --> aggregation-148,Serial9*)
  117-12; (*core-2,Serial3 --> aggregation-72,Serial2*)
  70-143; (*aggregation-88,Serial3 --> core-26,Serial9*)
  131-45; (*core-9,Serial0 --> aggregation-97,Serial9*)
  179-123; (*aggregation-41,Serial9 --> edge-47,Serial5*)
  136-84; (*aggregation-156,Serial5 --> edge-165,Serial2*)
  40-160; (*aggregation-110,Serial0 --> core-12,Serial5*)
  148-158; (*aggregation-53,Serial7 --> edge-57,Serial5*)
  163-102; (*aggregation-145,Serial10 --> edge-154,Serial1*)
  69-60; (*edge-92,Serial0 --> aggregation-84,Serial8*)
}

(*
{aggregation-137=0, aggregation-136=1, aggregation-135=2, edge-129=3, edge-126=4, edge-128=5, edge-127=6, aggregation-134=7, aggregation-133=8, aggregation-132=9, aggregation-73=10, edge-131=11, aggregation-72=12, edge-130=13, aggregation-75=14, aggregation-74=15, aggregation-77=16, aggregation-76=17, aggregation-125=18, aggregation-124=19, edge-119=20, edge-118=21, edge-115=22, edge-114=23, edge-117=24, edge-116=25, aggregation-123=26, aggregation-122=27, aggregation-121=28, aggregation-120=29, aggregation-60=30, aggregation-62=31, aggregation-61=32, aggregation-64=33, aggregation-63=34, aggregation-65=35, aggregation-113=36, aggregation-112=37, aggregation-111=38, edge-143=39, aggregation-110=40, edge-151=41, edge-150=42, edge-153=43, edge-152=44, aggregation-97=45, edge-80=46, aggregation-96=47, edge-81=48, aggregation-99=49, edge-82=50, aggregation-98=51, edge-83=52, aggregation-109=53, aggregation-108=54, edge-139=55, edge-138=56, aggregation-101=57, aggregation-100=58, edge-140=59, aggregation-84=60, edge-142=61, edge-141=62, edge-90=63, edge-95=64, aggregation-89=65, aggregation-86=66, edge-91=67, aggregation-85=68, edge-92=69, aggregation-88=70, edge-93=71, aggregation-87=72, edge-94=73, aggregation-39=74, edge-66=75, aggregation-38=76, edge-67=77, edge-68=78, edge-69=79, aggregation-173=80, aggregation-172=81, aggregation-171=82, edge-166=83, edge-165=84, edge-167=85, edge-175=86, edge-174=87, aggregation-37=88, aggregation-36=89, aggregation-169=90, aggregation-168=91, edge-78=92, edge-79=93, core-34=94, core-33=95, aggregation-161=96, core-32=97, aggregation-160=98, core-31=99, core-30=100, edge-155=101, edge-154=102, edge-162=103, edge-164=104, edge-163=105, aggregation-170=106, core-35=107, edge-70=108, edge-71=109, aggregation-159=110, aggregation-158=111, core-1=112, aggregation-157=113, core-0=114, core-3=115, edge-44=116, core-2=117, edge-107=118, edge-45=119, core-5=120, edge-46=121, core-4=122, edge-47=123, core-23=124, core-7=125, edge-104=126, core-22=127, core-6=128, edge-103=129, core-21=130, core-9=131, edge-106=132, core-20=133, core-8=134, edge-105=135, aggregation-156=136, edge-102=137, aggregation-51=138, core-29=139, aggregation-50=140, core-28=141, core-27=142, core-26=143, core-25=144, core-24=145, edge-42=146, edge-43=147, aggregation-53=148, aggregation-52=149, aggregation-149=150, edge-59=151, aggregation-148=152, aggregation-147=153, aggregation-146=154, edge-55=155, aggregation-49=156, edge-56=157, edge-57=158, edge-58=159, core-12=160, core-11=161, core-10=162, aggregation-145=163, edge-177=164, aggregation-144=165, edge-176=166, edge-179=167, edge-178=168, core-19=169, aggregation-40=170, core-18=171, core-17=172, core-16=173, core-15=174, core-14=175, core-13=176, aggregation-48=177, edge-54=178, aggregation-41=179}*)