(executable
  (name main)
  (libraries probNv findlib.dynload))

; Added to match https://github.com/ocaml/dune/blob/master/example/sample-projects/hello_world/bin/dune
(install
  (section bin)
  (files (main.exe as probNv)))
